[package]
name = "zzop-parser-typescript"
version.workspace = true
edition = "2021"
publish = false

[dependencies]
swc_core = { version = "71.0.5", features = ["ecma_parser", "ecma_ast", "ecma_visit", "common"] }
zzop-core = { path = "../../crates/core" }
regex = { workspace = true }
