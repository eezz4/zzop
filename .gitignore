# Rust build artifacts
/target
**/*.rs.bk
*.pdb

# All local-only measurement material in ONE place (never shipped):
#   corpus/oss/       — dogfood corpus checkouts (cloned OSS repos with embedded .git;
#                       re-acquisition commands live in the internal context docs)
#   corpus/benchmark/ — labeled detection-benchmark fixture (its own README)
#   corpus/*.json*    — local e2e configs/overlays
/corpus

# zzop report output (runtime artifact; --out / report.dir)
zzop-report.*/
/zzop-reports/

# OS / editor
.DS_Store
Thumbs.db
