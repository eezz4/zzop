{
  "id": "egress",
  "schema_version": 1,
  "rules": [
    {
      "id": "http-url-literal",
      "axis": "defect",
      "severity": "warning",
      "message": "A plain-`http://` URL STRING LITERAL in source. What is measured is the literal alone: nothing proves it is ever used as a request target (it may be an id, a docs link, or dead config), and nothing observes whether the page using it is served over https — so \"mixed content\" is the risk this names, not a fact it establishes. When the literal IS a request target from an https-served app, browsers block it as mixed content, or otherwise silently downgrade that request to no TLS. Use https, or route through an env-based/configurable base URL for internal service hosts. Loopback and private-range literals (`localhost`, `127.0.0.1`, `0.0.0.0`, `10.x`, `192.168.x`, `172.16-31.x`) are OUT OF SCOPE here, and on this rule's own yardstick rather than by deferral to anything else: the wire this rule names is a request that could cross a public network, and plain http to your own machine or your own private network is not that wire — there is no TLS to have been downgraded and no mixed-content block to warn about. Note what that exclude therefore does NOT reach: an internal hostname that resolves privately without a literal private IP, and an internal proxy that does require https, are both still flagged.",
      "matcher": {
        "type": "line-scan",
        "file_pattern": "(?i)\\.(ts|tsx|js|mjs|cjs|rs)$",
        "file_exclude_pattern": "${test-paths-stories}",
        "line_pattern": "['\"]http://[^'\"\\s]",
        "exclude_pattern": "(?i)(localhost|127\\.0\\.0\\.1|0\\.0\\.0\\.0|//192\\.168\\.|//10\\.[0-9]|//172\\.(1[6-9]|2[0-9]|3[01])\\.|xmlns|w3\\.org|schemas?\\.|\\.xsd)",
        "skip_comment_lines": true
      }
    },
    {
      "id": "get-and-body",
      "axis": "defect",
      "severity": "warning",
      "message": "A GET request carrying a body (a VALUE-position `method: 'get'` alongside `body:` in the same function) — servers/proxies may silently drop the body on a GET, so data the caller thinks it's sending never arrives. Use POST (or another body-bearing method) or move the data to query params. A TypeScript type-position `method: \"GET\" | \"POST\"` union (the common generic request-wrapper signature) is deliberately NOT matched — there the method is a parameter, not a committed GET, so the wrapper's conditional `body:` is fine. A file whose first 8 lines carry a machine-generated banner comment (`@generated`, `auto-generated`/`autogenerated`, `automatically generated`, `code generated by`, `this file is generated`, `this file was generated`, `openapi-generator`) is skipped WHOLE: the GET-with-body IS the generator's output, so switching it to POST or moving the data to query params is undone by the next regeneration. That banner list is COMPILED INTO this rule and does NOT read `vocabulary.generatedFileMarkers` — that config key reaches only the dead-code analyses (`unimported-export`, `dead-candidates`), so a house generator whose output carries a different banner — or no banner at all — is still flagged here, and the answer in either case is an `exclude` entry for its path.",
      "matcher": {
        "type": "method-scan",
        "file_pattern": "(?i)\\.(ts|tsx|js|mjs|cjs)$",
        "file_exclude_pattern": "${test-paths-stories}",
        "require_file_absent": ["(?i)\\A(?:[^\\n]*\\n){0,7}[ \\t]*(?://|/\\*|\\*)[^\\n]*(?:@generated|auto-?generated|automatically generated|code generated by|this file (?:is|was) generated|openapi-generator)"],
        "skip_comment_lines": true,
        "patterns": [
          { "pattern": "(?i)method\\s*:\\s*['\"]get['\"]\\s*(?:[,})]|$)", "label": "get-method" },
          { "pattern": "\\bbody\\s*:", "label": "body" }
        ],
        "trigger": "body"
      }
    },
    {
      "id": "ws-no-auth",
      "axis": "defect",
      "severity": "info",
      "message": "WebSocket opened/upgraded (`new WebSocket(...)`/`.upgrade(...)`) with no auth material (`token`/`auth`/`session`/`cookie`/`jwt`) in the same function — an unauthenticated realtime channel lets anyone who can reach the socket connect. The WebSocket API can't set custom headers on the handshake, so instead use a short-lived one-time ticket/token in the WS URL's query string exchanged server-side, rely on same-origin cookies (sent automatically on the upgrade), or authenticate via the first message sent over the socket.",
      "matcher": {
        "type": "method-scan",
        "file_pattern": "(?i)\\.(ts|tsx|js|mjs|cjs)$",
        "file_exclude_pattern": "${test-paths-stories}",
        "skip_comment_lines": true,
        "patterns": [
          { "pattern": "new\\s+WebSocket\\s*\\(|\\.upgrade\\s*\\(", "label": "ws" }
        ],
        "trigger": "ws",
        "absent": [
          { "pattern": "(?i)\\b(token|auth|session|cookie|jwt)\\b", "label": "auth" }
        ]
      }
    }
  ]
}
