# zzop documentation map

| Page                                                 | Answers                                                                                                                                                      |
| ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [extending.md](extending.md)                          | My stack does not match zzop out of the box — where do I plug in? The seven extension points in the order a user hits them, cheapest first, and what has no extension point at all.                          |
| [getting-started.md](getting-started.md)             | How do I install and run zzop on my repo, read the output, and silence a false positive?                                                                    |
| [demo/break-a-route.md](demo/break-a-route.md)       | Show me the point: change one backend route in an independently-authored FE/BE pair and watch zzop catch the contract drift on both sides while the frontend still compiles. Runnable: `bash docs/demo/break-a-route.sh`. |
| [recipes/verify-before-fetch.md](recipes/verify-before-fetch.md) | I'm an agent working in one repo and I need to know whether the endpoint I'm about to call exists in the other one — which calls to make before/after fetching it, and how to tell a real absence from zzop simply not having seen the other side. |
| [recipes/write-an-adapter.md](recipes/write-an-adapter.md) | zzop cannot see my stack and no native recognizer is coming — how do I inject the facts? Which channel the run itself tells me to fill, how small an envelope is allowed to be, what each channel costs in lines, and the before/after count that proves it worked. Worked end to end on a committed fixture. |
| [ARCHITECTURE.md](ARCHITECTURE.md)                   | How does a call to `analyze`/`analyzeTrees` process my tree — what's in the `ir` output, what does `degraded` mean, how does caching behave, what languages does the engine understand natively?  |
| [NORMALIZED_AST.md](NORMALIZED_AST.md)               | I'm writing an external/custom parser adapter (Ruby, JSP, ...) — what JSON envelope must it emit to join the analysis as a first-class source?      |
| [../examples/](../examples/README.md)                | What does a real Normalized AST envelope look like end-to-end, and how do I extend zzop? [`examples/adapters/`](../examples/adapters/README.md) is the current adapter list (the Mode-B references and the shared kit); the full JSP envelope sample (Mode A) is [`docs/contracts/example-envelope.json`](contracts/example-envelope.json), the same bytes `zzop contract example-envelope` serves; [`cases/`](../cases/README.md) is the labeled detection benchmark those rules are scored against. |
| [modules/mcp.md](modules/mcp.md)                     | How do the Node-free `zzop`/`zzop-mcp` binaries expose the same engine — MCP tool surface, embedded authoring-contract resources, CLI mode, bundled packs, config semantics, and the summary/truncation output contract. |
| [modules/facade.md](modules/facade.md)               | I'm embedding the engine directly — what is the `zzop-facade` JSON contract (every entry point, request/response JSON shapes, error handling)? |
| [rules/dsl-reference.md](rules/dsl-reference.md)     | What is the exact JSON schema for a DSL rule pack (`rules/dsl/<pack>/<pack>.json`) — every matcher field, suppress-marker semantics, schema-version policy, finding shape? |
| [rules/authoring-guide.md](rules/authoring-guide.md) | How do I write and ship a new DSL rule pack — placement, a worked example, performance pitfalls, the testing/fidelity bar, and when to write a native rule instead? |
| [rules/catalog.md](rules/catalog.md)                 | What rules ship today — every DSL pack's rules and every native analysis id, with what each one detects?                                                    |
| [rules/linter-overlap.md](rules/linter-overlap.md)   | What does zzop add over the linters I already run — per bundled rule, the nearest standard-linter equivalent, or none?                                       |
| [adapters/README.md](adapters/README.md)             | I'm writing a non-Rust adapter — how do I prove my HTTP interface-key normalization is byte-identical to the engine's, so cross-layer joins don't silently fail? |
