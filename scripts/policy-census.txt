crates/cache/src/evict.rs:EVICT_TO_RATIO_DEN cap # hysteresis denominator — see EVICT_TO_RATIO_NUM
crates/cache/src/evict.rs:EVICT_TO_RATIO_NUM cap # hysteresis numerator — one eviction pass reclaims to 3/4 of the cap rather than exactly to it, so the directory does not sit AT the budget and pay for a full pass every run. Paired with EVICT_TO_RATIO_DEN; neither is meaningful alone
crates/cache/src/evict.rs:MAX_CACHE_BYTES cap # disk budget for one cache directory. A threshold, not a name — and the ONLY thing bounding growth since the schema version stopped carrying the release number (before that, every upgrade wiped and that wipe was the reclamation). 256 MiB sits far above any single tree's LIVE set, so in practice the evictor only reaches entries already orphaned by an edit. The mean entry size that makes that sentence true is a MEASUREMENT, not an axis verdict: it has one owner at the declaration, which carries the recount command beside it — this tail said `~6 KB/entry` / `~44k entries` while the declaration was correcting exactly those two numbers as ~3x off in the same commit (`e7b20da`), and the key-only comparison this guard performs can never catch a false tail. Deliberately not a config key yet: the trigger for making it declarable is a user who actually hits it
crates/cache/src/hash.rs:FNV_OFFSET_A internal # FNV-1a basis; changing it re-addresses every cache entry filename
crates/cache/src/hash.rs:FNV_OFFSET_B internal # FNV-1a basis; changing it re-addresses every cache entry filename
crates/cache/src/hash.rs:FNV_PRIME_A internal # FNV-1a basis; changing it re-addresses every cache entry filename
crates/cache/src/hash.rs:FNV_PRIME_B internal # FNV-1a basis; changing it re-addresses every cache entry filename
crates/cache/src/lib.rs:DEFAULT_CACHE_DIR internal # zzop's own on-disk layout; already overridable via the cacheDir config key
crates/cache/src/lib.rs:TOOL_DIR internal # zzop's own on-disk namespace (.zzop/); the walk prunes the RESOLVED cache dir, not this name
crates/cache/src/store.rs:FINDINGS_DIR internal # cache-directory layout zzop defines
crates/cache/src/store.rs:FORMAT_VERSION internal # on-disk entry format version
crates/cache/src/store.rs:IR_DIR internal # cache-directory layout zzop defines
crates/cache/src/store.rs:SCHEMA_VERSION_FILE internal # cache-directory layout zzop defines
crates/cache/src/store/tests.rs:READS test # how many times the healthy-cache test re-reads one entry before concluding it is never refused. A fixture repetition count, not policy: the pre-fix defect refused an entry on the FIRST read, so any value above 1 reproduces it — 100 is headroom for a rarer nondeterminism, not a tuned threshold
crates/cache/src/store/tests.rs:VERSIONS test # the schema-version strings the equality-vs-ordering matrix test opens a cache under. Fixture inputs, not policy: shaped like real values (a bare hash, plus one legacy release+hash spelling so the transition off that format stays covered)
crates/config/src/lib.rs:CONFIG_SURFACE_JSON internal
crates/config/src/lib.rs:DEFAULT_AUTHORED_PACKS_DIR internal # zzop's own on-disk layout; already overridable via packs.extraDirs
crates/config/src/lib.rs:DEFAULT_CONFIG_FILENAME internal
crates/config/src/lib_tests.rs:TREES_LESS_CONFIG test # fixture config body for the single-tree-over-a-workspace disclosure tests
crates/config/src/load.rs:MISSING_CONFIG_MARKER internal # the stable head of zzop's own refusal message, matched by its own hosts to append their prescription — not a user-tunable knob
crates/config/src/mapper/options.rs:FRONT_END_ONLY_VOCABULARY_KEYS internal # zzop's own config keys the front end consumes rather than forwards
crates/config/src/mapper/severity.rs:SEVERITY_ALIASES internal # zzop's own accepted severity tokens (off/critical/warn/...)
crates/config/src/mapper/warnings.rs:RETIRED_KEYS internal # config keys zzop itself retired
crates/config/src/template.rs:CONFIG_TEMPLATE_JSONC internal
crates/config/src/template_tests.rs:ANCHOR test # the sentence the pack-id guard locates its subject by. Not a policy value — it is a NEEDLE into template.rs prose, and it is asserted-on rather than trusted: if the template stops containing it the guard panics naming itself, because a needle that silently matches nothing turns the whole check vacuously green
crates/config/src/workspaces.rs:MAX_GLOB_DEPTH cap
crates/core/src/call_sites.rs:CALL_KIND_CONSOLE_WRITE internal # zzop's own wire spelling for a call-site family, same shape as the sentinel-kind consts below — a project cannot rename it into meaning something else, and no external spec fixes it. What the family CONTAINS per language (which console/logger call counts) is the producer's judgment, and that judgment is where the convention-shaped vocabulary will live if any of it ever becomes declarable
crates/core/src/call_sites.rs:CALL_KIND_ENV_READ internal # sibling of CALL_KIND_CONSOLE_WRITE, same reasoning
crates/core/src/call_sites.rs:CALL_KIND_HASH_CALL internal # sibling of the other CALL_KIND_* wire spellings, same reasoning — zzop own name for a call-site family; which spellings each language folds into it is each producer judgment
crates/core/src/call_sites.rs:CALL_KIND_PROCESS_EXEC internal # sibling of CALL_KIND_CONSOLE_WRITE/ENV_READ, same reasoning — zzop own wire spelling for a call-site family; what the family CONTAINS per language is each producer judgment
crates/core/src/call_sites.rs:RULE_READ_CALL_KINDS internal # the call kinds this build's RULES read — the call-side twin of RULE_READ_IO_KINDS below, same axis for the same reason (a description of OUR wiring, not a name a project picks). WHICH kinds are in it, which wave earned each one, and which declarative surfaces its binding test (crates/engine/tests/rule_contracts/call_kind_readers.rs) reads are all owned by the declaration's own doc — restated here they would go stale silently, since this guard compares key and axis only and can never read a tail. Do not re-add a membership list or a test description to this row — nothing in this repo can tell you when it stops being true
crates/core/src/dsl/fragments.rs:SHARED_FRAGMENTS_JSON internal # embedded DSL bundle; fragment NAMES are censused separately in Rust
crates/core/src/dsl/fragments.rs:TEST_PATH_FRAGMENT_PREFIX internal # the NAME of zzop's own shared fragment family (`${test-paths}` and its `test-paths-*` extensions), not a name any project picks -- a project's own test paths are `vocabulary.extraTestPathPatterns`
crates/core/src/dsl/inline_census_tests.rs:AXES test # the inline-value census axis vocabulary + its legend; a guard vocabulary, not shipped policy
crates/core/src/dsl/inline_census_tests.rs:CENSUS_FILE test # path of the committed inline-value census snapshot
crates/core/src/dsl/inline_census_tests.rs:CONVENTION_CEILING test # ratchet on the inline-value census convention axis (lower only) — each row is a config key that does not exist yet or one the pack is not wired to read
crates/core/src/dsl/inline_census_tests.rs:SUBJECT_FLOOR test # vacuous-green floor on the inline-value census subject set
crates/core/src/dsl/inline_census_tests.rs:UNREVIEWED_CEILING test # ratchet on the inline-value census unreviewed axis (lower only)
crates/core/src/dsl/inline_census_tests.rs:UPDATE_CMD test # the regeneration command, spelled once so every failure message can print it
crates/core/src/dsl/inline_census_tests.rs:UPDATE_ENV test # env var that switches the inline-value census from check to regenerate
crates/core/src/dsl/markers/mod.rs:MARKER_LOOKBACK_LINES cap
crates/core/src/dsl/markers/mod.rs:NEAR_MISS_MARKER_TOKEN_PATTERN internal # grammar of zzop's own <id>-ok suppression marker
crates/core/src/dsl/markers/path.rs:HASH_COMMENT_EXTENSIONS fact # extensions in which # is a comment leader and // is not a comment at all — a config format's syntax or a language's, fixed by whoever defined it and never a name a project picks, so a suppress marker written there must use #. The MEMBERS are not restated here: the declaration is their only owner (`marker_widening_prose` derives the shipped roster from it), and every hand-written copy of that list so far has gone stale by omitting .py
crates/core/src/dsl/source/line_shape.rs:BLOB_LINE cap
crates/core/src/dsl/source/line_shape.rs:LONG_LINE cap
crates/core/src/dsl/tests_call_scan.rs:SRC test # the fixture source text the call-scan interpreter tests hand-place sites against
crates/core/src/dsl/tests_fragments/name_census.rs:CENSUSED_FRAGMENTS test
crates/core/src/dsl/tests_fragments/name_census.rs:SHARED_BUNDLE test
crates/core/src/dsl/tests_fragments/superset.rs:BASE test
crates/core/src/dsl/tests_http_conventions.rs:HTTP_CONVENTIONS_JSON test
crates/core/src/dsl/tests_literal_scan.rs:SRC test
crates/core/src/dsl/tests_markers/near_miss_tests.rs:BASE_MESSAGE test
crates/core/src/dsl/tests_method_scan_same_fn.rs:SIBLING_CLOSURES test
crates/core/src/dsl/veto_window.rs:MAX_CALL_WINDOW_LINES cap # how far past a call site its lexical VETO window may reach. A bound on a suppression, so the conservative direction is small: a declaration a formatter pushed onto the next line is the case it exists for, while a match eight lines down is far likelier to belong to something else. Not a tuning knob a project would want different — a call whose arguments span more than eight lines is not the shape any rule using this field is written for
crates/core/src/dsl/veto_window/enclosing.rs:MAX_ENCLOSING_WALK_LINES cap # how far ABOVE a matched line the enclosing-opener walk may look. A bound on a suppression, so the conservative direction is small — but the risk this number carries is bounded by CONSTRUCTION rather than by its size: only lines carrying a bracket still open at the site enter the window, so raising it adds candidate LINES, never candidate TEXT, and a veto keyword cannot hide in the statements between. 40 is headroom over the farthest opener->finding distance measured on cal.com @ b25beb5 (23 lines: apps/api/v2/src/modules/tokens/tokens.repository.ts:100 -> :123; recount with `zzop analyze --rule db/unawaited-write`) and far short of a function body, which is the distance at which a lexical veto starts speaking for code it has no claim on. Not a tuning knob a project would want different
crates/core/src/file_nodes.rs:HOTSPOT_MIN_CHANGES cap
crates/core/src/io/facts.rs:RULE_READ_IO_KINDS internal # the io kinds this build's RULES read — the set the S11 unread-io-kind self-report subtracts against. Renamed from JOINED_IO_KINDS on 2026-08-01: the old name claimed a join-membership rule the code never computed (link_cross_layer_io joins on the composite (kind,key) and compares the kind to nothing, so an unread kind DOES join), and the S11 message inherited that false claim. Not `convention` (a project cannot declare a kind into being read) and not `fact` (no external spec fixes them): it is a description of OUR own wiring, and rule_contracts::io_kind_readers binds it to the kind literals production source compares, both directions — a scan that can only ever prove the rules half
crates/core/src/io/key.rs:HTTP_KEY_VERBS fact # HTTP method tokens, fixed by RFC
crates/core/src/io/key.rs:UNKNOWN_VERB internal
crates/core/src/ir.rs:DEP_GRAPH_RESOLVED_ONLY internal # one owner for a disclosure sentence several surfaces quote verbatim: the dep channel carries only RESOLVED in-tree edges. Not a tunable but a description of what that channel is, and changing the sentence turns every quoting site red by verbatim comparison
crates/core/src/node.rs:DAY_MS test
crates/core/src/node.rs:DEFAULT_RECENT_THRESHOLD_DAYS cap
crates/core/src/node.rs:DEFAULT_WEIGHTS cap # numeric risk-score weights, not a name
crates/core/src/node.rs:MS_PER_DAY internal # unit arithmetic, not a name at all
crates/core/src/node.rs:NOW test
crates/core/src/noncycle.rs:CYCLE_GRAPH_EXCLUDES_ERASED_IMPORTS internal # one owner for a disclosure sentence the two `zzop graph` dep lanes quote verbatim: the cycle verdict is computed after this module subtracts compile-time-erased imports, while the edge list they emit is not. Not a tunable but a description of the seam between two channels, and it sits here rather than in `ir.rs` beside DEP_GRAPH_RESOLVED_ONLY because the subtraction it describes is this file's
crates/core/src/noncycle.rs:ERASING_EXTENSIONS fact # the file extensions whose `export type`/`export interface` TypeScript erases — fixed by the language, not a tunable. Its JOB is to keep the export-side cycle exclusion off Java/Rust/Go, whose `interface`/`trait` collapse onto the same SourceSymbolKind and are NOT erased; widening it would silently delete real cycles there
crates/core/src/normalized/tests.rs:BINDS_UTIL test # fixture JSON body for the overrides contract tests
crates/core/src/normalized/version.rs:MIN_VERSION_FOR_CALLS internal # contract version gate, not a tunable: it must equal the version in which the `calls` channel shipped, or a mislabeled envelope would smuggle the channel past the floor silently
crates/core/src/normalized/version.rs:MIN_VERSION_FOR_OVERRIDES internal # contract version gate, not a tunable: it must equal the version in which `overrides` shipped, or an older engine ignores the declaration silently
crates/core/src/normalized/version.rs:MIN_VERSION_FOR_ROUTER_MOUNT_REF internal # contract version gate, not a tunable: it must equal the version in which the RouterMountEntry::MountRef variant shipped, and a later contract bump must not carry it along
crates/core/src/normalized/version.rs:NORMALIZED_AST_CONTRACT_VERSION internal # the release in which the envelope shape last changed, not a tunable: it is a fact about this repo's own history and a wrong value makes conforming envelopes unreadable
crates/core/src/normalized/version.rs:NORMALIZED_AST_FORMAT internal
crates/core/src/normalized/version.rs:SUPPORTED_NORMALIZED_AST_VERSION internal
crates/core/src/pack_loader.rs:SUPPORTED_DSL_SCHEMA_VERSION internal
crates/core/src/pack_loader/retired_fields.rs:RETIRED_RULE_FIELDS internal # fields zzop ITSELF once accepted on a rule and retired, each with its remedy. Not `convention` (a pack author cannot rename them into existence) and not `fact` (no external framework fixed them) — it is our own retired surface, and the list only ever grows when WE retire something
crates/core/src/recognizer.rs:AUTH_EVIDENCE internal # spelling of the one non-io recognizer channel (auth-guard evidence, the decorator-guard side channel that exempts routes from mutating-route-no-auth). Same closed FrameworkRecognizer vocabulary as its three io siblings and NOT `convention` for the same reason: fixed by our own producer/consumer seam, no user may rename it
crates/core/src/recognizer.rs:CONSUMES internal # wire spelling of one cross-layer channel, the closed vocabulary a FrameworkRecognizer declaration may name. NOT `convention`: a convention is a name the PROJECT picks and declares in config, while this one is fixed by our own producer/consumer channel and no user may rename it
crates/core/src/recognizer.rs:DB_CONSUMES internal # wire spelling of one cross-layer channel, the closed vocabulary a FrameworkRecognizer declaration may name. NOT `convention`: a convention is a name the PROJECT picks and declares in config, while this one is fixed by our own producer/consumer channel and no user may rename it The db kind is spelled once per SIDE (2026-08-26): one spelling for both made a consume-only recognizer declare a provide-named channel, and the coverage cross that counts the sides separately listed it as capability for the side it never fills.
crates/core/src/recognizer.rs:DB_PROVIDES internal # wire spelling of one cross-layer channel, the closed vocabulary a FrameworkRecognizer declaration may name. NOT `convention`: a convention is a name the PROJECT picks and declares in config, while this one is fixed by our own producer/consumer channel and no user may rename it The db kind is spelled once per SIDE (2026-08-26): one spelling for both made a consume-only recognizer declare a provide-named channel, and the coverage cross that counts the sides separately listed it as capability for the side it never fills.
crates/core/src/recognizer.rs:PROVIDES internal # wire spelling of one cross-layer channel, the closed vocabulary a FrameworkRecognizer declaration may name. NOT `convention`: a convention is a name the PROJECT picks and declares in config, while this one is fixed by our own producer/consumer channel and no user may rename it
crates/core/src/registry/redact.rs:REDACTED internal # the marker an excluded evidence path is replaced with; zzop's own output vocabulary, not a tunable
crates/core/src/rule_channels.rs:ALL internal # the closed set of the six channel spellings above, asserted equal to the side-by-kind cross product of RULE_READ_IO_KINDS by that module's own test — a channel missing here is one no rule can declare and no disclosure can group by
crates/core/src/rule_channels.rs:DB_TABLE_CONSUMES internal # wire spelling of one native-rule io channel (a cross-layer side paired with an io kind), the closed vocabulary a NATIVE_ANALYSES row may name. Same axis and same argument as the recognizer.rs PROVIDES/CONSUMES spellings it is composed from: fixed by our own producer/consumer channel, and no user may rename it
crates/core/src/rule_channels.rs:DB_TABLE_PROVIDES internal # wire spelling of one native-rule io channel (a cross-layer side paired with an io kind), the closed vocabulary a NATIVE_ANALYSES row may name. Same axis and same argument as the recognizer.rs PROVIDES/CONSUMES spellings it is composed from: fixed by our own producer/consumer channel, and no user may rename it
crates/core/src/rule_channels.rs:HTTP_CONSUMES internal # wire spelling of one native-rule io channel (a cross-layer side paired with an io kind), the closed vocabulary a NATIVE_ANALYSES row may name. Same axis and same argument as the recognizer.rs PROVIDES/CONSUMES spellings it is composed from: fixed by our own producer/consumer channel, and no user may rename it
crates/core/src/rule_channels.rs:HTTP_PROVIDES internal # wire spelling of one native-rule io channel (a cross-layer side paired with an io kind), the closed vocabulary a NATIVE_ANALYSES row may name. Same axis and same argument as the recognizer.rs PROVIDES/CONSUMES spellings it is composed from: fixed by our own producer/consumer channel, and no user may rename it
crates/core/src/rule_channels.rs:NO_IO internal # the empty channel set, named once so a rule whose evidence is not io at all states that rather than defaulting to it. Holds no vocabulary by construction; censused because it is the shape every rules-graph/rules-schema row points at, and rule_contracts::rule_channels checks the claim crate-wide
crates/core/src/rule_channels.rs:TRPC_CONSUMES internal # wire spelling of one native-rule io channel (a cross-layer side paired with an io kind), the closed vocabulary a NATIVE_ANALYSES row may name. Same axis and same argument as the recognizer.rs PROVIDES/CONSUMES spellings it is composed from: fixed by our own producer/consumer channel, and no user may rename it
crates/core/src/rule_channels.rs:TRPC_PROVIDES internal # wire spelling of one native-rule io channel (a cross-layer side paired with an io kind), the closed vocabulary a NATIVE_ANALYSES row may name. Same axis and same argument as the recognizer.rs PROVIDES/CONSUMES spellings it is composed from: fixed by our own producer/consumer channel, and no user may rename it
crates/core/src/string_literals.rs:FNV_OFFSET internal # FNV-1a 64 basis for BoundStringLiteral::value_hash; changing it invalidates every cached value_hash and the rule-side value==name comparison, never a tunable
crates/core/src/string_literals.rs:FNV_PRIME internal # FNV-1a 64 prime, same contract as FNV_OFFSET directly above
crates/core/src/string_literals.rs:HIGH_ENTROPY_SECRET_MIN_BITS cap # measured entropy floor (T2, 2026-08-03) for security/high-entropy-secret; the pack ships the evaluated copy and rule_contracts/literal_scan_threshold.rs pins the two equal
crates/engine/build.rs:FNV_OFFSET internal # FNV-1a basis; changing it re-addresses every derived fingerprint (= one cache wipe)
crates/engine/build.rs:FNV_PRIME internal # FNV-1a prime; same consequence as the basis above
crates/engine/src/analyze/assemble/helpers/csharp.rs:CSHARP_STD_IMPORT_HEADS fact # .NET BCL namespace roots
crates/engine/src/analyze/assemble/helpers/java.rs:JAVA_STD_IMPORT_HEADS fact # JDK package roots
crates/engine/src/analyze/assemble/helpers/rust.rs:RUST_STD_CRATE_FAMILY fact # Rust's own std crate names
crates/engine/src/analyze/assemble/nuxt_auto_import.rs:AUTO_IMPORT_EXTS fact # the extensions Nuxt's own auto-import scanner reads under an auto-import directory
crates/engine/src/analyze/assemble/nuxt_auto_import.rs:NUXT_DEFAULT_AUTO_IMPORT_DIRS fact # the directories Nuxt auto-imports with NO declaration anywhere -- fixed by the framework. Directories a project's own nuxt.config.* adds are read from that declaration instead (parse_nuxt_imports_dirs), never listed here
crates/engine/src/analyze/assemble/nuxt_auto_import/referrers.rs:NUXT_PUBLIC_DIR fact # the directory Nuxt copies to the site root UNTRANSFORMED -- fixed by the framework, which is why no token under it can be an auto-import call site. Not a project-named path: an app that serves a different directory does so by configuring Nuxt, and that configuration is not read here
crates/engine/src/analyze/assemble/nuxt_auto_import/referrers.rs:NUXT_TOKEN_ROSTER_EXTS fact # which pre-scan filetype is a Nuxt COMPONENT and therefore a place a bare auto-imported name resolves -- fixed by the framework. Deliberately narrower than zzop_parser_typescript::PRESCAN_IMPORT_HOSTS: .svelte/.astro are not Nuxt modules and a .md/.mdx page compiles only its <script setup> block, not its prose
crates/engine/src/analyze/assemble/rules/framework_entries.rs:NUXT_ENTRY_DIRS fact # the directories Nuxt auto-registers by name; a project cannot rename them
crates/engine/src/analyze/compose/client_base.rs:SENTINEL_KIND internal # alias of zzop's own sentinel kind
crates/engine/src/analyze/compose/global_prefix.rs:SENTINEL_KIND internal # alias of zzop's own sentinel kind
crates/engine/src/analyze/diagnostics.rs:SAMPLE cap
crates/engine/src/analyze/diagnostics/capability.rs:ON_RAMP_EXT_SAMPLE cap
crates/engine/src/analyze/diagnostics/config_filters.rs:NOT_SOURCE_BY_CONSTRUCTION fact # directory names the skipDirs disclosure never reports. FACT, not convention: each is fixed by a tool (git's object store, zzop's own former report/cache dirs) and no project can rename it or put analyzable source in it — which is precisely why it is safe to withhold. Every other name in a skip list IS a convention and is reported
crates/engine/src/analyze/diagnostics/pack_scope/uncovered_extension.rs:MIN_UNCOVERED_EXTENSION_SHARE_PCT cap # percent-of-tree floor above which an extension counts as a PRINCIPAL filetype for the "no loaded DSL rule targets it" self-report. A number, not a name — nothing about a project's own vocabulary is spellable here, and a project that wants the report on a smaller slice wants a different threshold, not a different word
crates/engine/src/analyze/diagnostics/pack_scope/uncovered_extension.rs:THIN_RULE_REACH_PCT cap # percent-of-the-loaded-rule-set floor above which a principal filetype counts as broadly covered rather than thinly reached. A number, not a name — nothing a project spells is declarable here, and a project wanting the report on a wider slice wants a different threshold. Its value is read off a per-language REACH measurement whose ONE owner is the declaration, which carries the table and the recount command beside it. The row that stood in this tail until 2026-08-31 was a verbatim copy of the reading the declaration had already RETRACTED as non-reproducing (four of six entries and the denominator wrong), and no guard could have said so: this census compares key and axis only and can never read a tail — the same trap `crates/cache/src/evict.rs:MAX_CACHE_BYTES` records two hundred rows above. Do not re-add the numbers here
crates/engine/src/cache.rs:CACHE_SCHEMA_VERSION internal
crates/engine/src/cache.rs:DSL_INTERPRETER_FINGERPRINT internal
crates/engine/src/cache.rs:LEXICAL_FALLBACK_ID internal # names which projector produced an entry; the version half is a source hash (build.rs)
crates/engine/src/channel_direction/table.rs:OBSERVED internal # zzop's own native analysis IDS paired with the io channel each reads and the MEASURED response of that rule to the channel being empty (silences / floods / unmoved). Not a threshold and not user vocabulary — the same rule-id space as the NATIVE_ANALYSES tables, and every value here is re-derived by rule_contracts::channel_direction on each test run rather than chosen. A row silently dropped removes a rule from a user-facing sentence about what this run could not see
crates/engine/src/cross_layer_findings/diagnosed.rs:DIAGNOSING_RULE_IDS internal # zzop rule ids, not user vocabulary — which of our own rules count as a specific diagnosis
crates/engine/src/dead_exports/covered_extensions_pin.rs:NON_TS_CALL_GRAPH_EXTENSIONS fact # file extensions fixed by the languages
crates/engine/src/disclosure/registry/analysis.rs:ROWS internal # one taxonomy group of zzop's own disclosure-class registry (ids, groups, prose). Split from registry.rs on the file-line cap 2026-08-17 — same axis, same rows
crates/engine/src/disclosure/registry/extraction.rs:ROWS internal # one taxonomy group of zzop's own disclosure-class registry (ids, groups, prose). Split from registry.rs on the file-line cap 2026-08-17 — same axis, same rows
crates/engine/src/disclosure/registry/input.rs:ROWS internal # one taxonomy group of zzop's own disclosure-class registry (ids, groups, prose). Split from registry.rs on the file-line cap 2026-08-17 — same axis, same rows
crates/engine/src/disclosure/registry/trust.rs:ROWS internal # one taxonomy group of zzop's own disclosure-class registry (ids, groups, prose). Split from registry.rs on the file-line cap 2026-08-17 — same axis, same rows
crates/engine/src/disclosure/tests.rs:EXPECTED test
crates/engine/src/disclosure/types.rs:ANALYSIS_DARK internal
crates/engine/src/disclosure/types.rs:EXTRACTION_BLIND internal
crates/engine/src/disclosure/types.rs:INPUT_CONFIG internal
crates/engine/src/disclosure/types.rs:TRUST_CALIBRATION internal
crates/engine/src/dispatch.rs:DEFAULT_SKIP_DIRS convention -> vocabulary.skipDirs # dist/build/.next/target are build-output names a project picks; decides what is never analyzed at all
crates/engine/src/dispatch/non_source.rs:NON_SOURCE_EXTENSIONS fact # extensions fixed by the file formats
crates/engine/src/dispatch/tests.rs:EXPECTED test
crates/engine/src/envelope/callgraph/disclosure.rs:CALL_GRAPH_RULE_IDS internal # zzop's own rule ids, named in the envelope call-graph absence warning and pinned against the shipped registry — not a name any project chooses
crates/engine/src/envelope/callgraph/disclosure.rs:MAX_DROP_EXAMPLES cap # per-warning example-file cap in the dropped-calls disclosure, not a name
crates/engine/src/envelope/resolve.rs:EXTENSIONLESS_CANDIDATE_EXTENSIONS fact # extensions a resolver may append to an extensionless specifier — fixed by TS/bundler classic resolution, not by any project
crates/engine/src/file_routes/mod.rs:HTTP_VERB_EXPORTS fact # Next.js App Router requires these exact export names
crates/engine/src/file_routes/mod.rs:ROUTE_EXTENSIONS fact # what a framework SERVES as a route is fixed by the framework, not named by any project — that is the fact axis, and the declaration's own doc owns which conventions were read and which entries each one fixes. AMBIGUOUS on two counts, both stated at the declaration and neither restatable here safely: Next's pageExtensions is overridable per project (the override renames nothing, it narrows/widens which language-fixed extensions Next serves, and zzop tracks the default), and the list is deliberately WIDER than every convention's default by a documented notch of over-inclusion — so part of this set is zzop's own generosity rather than anything a framework fixed. Read the declaration before citing this row as purely framework-fixed
crates/engine/src/file_routes/tests.rs:NOT_ROUTED_BY_ANY_CONVENTION test
crates/engine/src/file_routes/tests.rs:TYPESCRIPT_DISPATCH_EXTENSIONS test
crates/engine/src/framework_silence/builtin_fetch.rs:FETCH_CALL_SITES_MIN cap
crates/engine/src/framework_silence/builtin_fetch.rs:FUNNEL_TAIL internal # warning prose zzop emits
crates/engine/src/framework_silence/builtin_fetch.rs:MAX_SAMPLES cap
crates/engine/src/framework_silence/call_graph_language.rs:MAX_EXAMPLES cap
crates/engine/src/framework_silence/call_graph_language.rs:SILENCED_RULE_ID internal # zzop rule id, and THE owner of that spelling in this module tree since 2026-08-31: S17 (`route_language_zero_extraction`) named the same rule in its own literal while its doc claimed this module's registry pin covered it -- it never did, and a ghost value there left all 140 framework_silence tests green. S17 reads this constant now, so the pin beside it guards both warnings
crates/engine/src/framework_silence/client_library_import.rs:HTTP_CLIENT_SPECIFIERS fact # published package names
crates/engine/src/framework_silence/committed_spec_io_silence.rs:IO_NEAR_ZERO_FLOOR cap
crates/engine/src/framework_silence/committed_spec_io_silence.rs:MAX_SPEC_PROBES cap
crates/engine/src/framework_silence/controller_silence.rs:MAX_SAMPLES cap
crates/engine/src/framework_silence/controller_silence.rs:MIN_FILES cap
crates/engine/src/framework_silence/controller_silence.rs:MIN_PROVIDES_FLOOR cap
crates/engine/src/framework_silence/csharp_base_route.rs:BASE_LIST_LOOKAHEAD cap # lines below a class name that S13 will scan for a wrapped base-list `:`. A bound, not a judgement about C#: two covers every wrapped spelling seen, and it keeps the scan off a minified file
crates/engine/src/framework_silence/csharp_base_route.rs:FRAMEWORK_BASES fact # ASP.NET Core base controller types, named by Microsoft. Deriving from one of these says nothing about an inherited [Route] — the S13 warning fires only on a base OUTSIDE this list, because every ASP.NET controller derives from one of them and warning on all of them would name no hazard
crates/engine/src/framework_silence/csharp_base_route.rs:MAX_EXAMPLES cap # controller classes named per S13 warning. The COUNT is always exact; only the example list is capped
crates/engine/src/framework_silence/egress_intent.rs:ARG_SCAN_CAP cap
crates/engine/src/framework_silence/fetch_wrapper.rs:MAX_EXAMPLES cap
crates/engine/src/framework_silence/fetch_wrapper.rs:WRAPPER_EXPORT_NAMES convention -> vocabulary.fetchWrapperExportNames # binding names a project's own hand-rolled http wrapper module exports (api, http, client, del)
crates/engine/src/framework_silence/fetch_wrapper.rs:WRAPPER_FUNNEL_TAIL internal # warning prose zzop emits
crates/engine/src/framework_silence/gateway_declaration.rs:GATEWAY_DECLARATIONS fact # deployment-declaration filenames and the constructs inside them that can move a route path (Vercel rewrites, Next basePath, nginx proxy_pass) — spelled by those tools, not by us. The markers are what keep the S12 report from being a filename census: the file must actually declare a rewrite, because warning on mere presence would train the reader to skip the line
crates/engine/src/framework_silence/orm_schema_silence.rs:MAX_EXAMPLES cap
crates/engine/src/framework_silence/orm_schema_silence.rs:ORM_SCHEMA_SPECIFIERS fact # published package names
crates/engine/src/framework_silence/orm_schema_silence.rs:SILENCED_RULE_ID internal # zzop rule id
crates/engine/src/framework_silence/partial_route_silence.rs:MIN_SILENT_FILES cap
crates/engine/src/framework_silence/partial_route_silence.rs:SAMPLE cap
crates/engine/src/framework_silence/python_mount_prefix.rs:MAX_CALL_SPAN cap # bytes S14 will scan forward for an include_router call closing paren. Not a judgement about Python: a mount call is a few lines, and the bound keeps one unbalanced paren in a generated file from becoming a whole-file scan
crates/engine/src/framework_silence/python_mount_prefix.rs:MAX_EXAMPLES cap # mount sites named per S14 warning. The COUNT stays exact; only the example list is bounded
crates/engine/src/framework_silence/rust_router_layer.rs:AFFECTED_RULE_ID internal # zzop rule id
crates/engine/src/framework_silence/rust_router_layer.rs:MAX_EXAMPLES cap
crates/engine/src/framework_silence/server_framework_import.rs:SERVER_FRAMEWORK_SPECIFIERS fact # published package names
crates/engine/src/framework_silence/unknown_verb_range.rs:MAX_EXAMPLES cap
crates/engine/src/framework_silence/unknown_verb_range.rs:SILENCED_RULE_ID internal # zzop rule id
crates/engine/src/framework_silence/unread_io_kind.rs:MAX_EXAMPLES cap # example files named per unread kind in the S11 warning — enough to go look, bounded so one adapter's thousand rows cannot crowd out the run's other warnings. The COUNT is always exact; only the example list is capped
crates/engine/src/generated_banner.rs:MARKERS convention -> vocabulary.generatedFileMarkers # generated-file banner text; a codegen tool or a team can word its banner any way
crates/engine/src/io.rs:DEFAULT_ROUTER_NAMES convention -> vocabulary.routerNames # the identifier a project gives its own router value; its own doc called it an escape hatch while nothing could reach it
crates/engine/src/lib.rs:DEFAULT_SIZE_CAP cap
crates/engine/src/pipeline/rust_workspace.rs:TARGET_SECTIONS fact # Cargo.toml section headers fixed by Cargo
crates/engine/src/tests.rs:DERIVED_SCHEMA_GATE test
crates/engine/src/trees/cross_tree_imports.rs:MAX_EXAMPLES cap # specifiers named per cross-tree-import disclosure. Same shape as every framework_silence MAX_EXAMPLES: the COUNT is always exact and only the example list is bounded, and the message says out loud that it truncated (a silent cut would read as "that was all of them")
crates/engine/src/trees/parallel_impl.rs:MIN_PARALLEL_IMPL_SIGNALS cap
crates/engine/src/trees/unreported_read_drift.rs:SAMPLE cap
crates/engine/src/vocabulary.rs:DEFAULT_JAVA_SOURCE_ROOT convention -> vocabulary.javaSourceRoot # the Maven/Gradle layout is a source root a project can relocate (moved here from decorator_gate.rs SRC_ROOT, whose one reader now takes it as an argument)
crates/engine/src/vocabulary/normalizers.rs:NORMALIZED_VOCABULARY_KEYS internal # zzop's own config-key spellings paired with the transform each key's consuming rule applies before comparison — a description of OUR wiring, same axis as the CALL_KIND_* wire spellings. A project cannot rename these keys. The census matters here because a key silently MISSING from this table restores exactly the silent failure the table was built to end (a declared entry that can never match, with no warning), and nothing else notices the omission
crates/git/src/parse.rs:MS_PER_DAY internal # unit arithmetic (24*60*60*1000), neither a name nor a threshold
crates/git/src/process.rs:COMMIT_MARKER internal # in-band marker in zzop's own git-log --format string
crates/git/src/tests.rs:FAR_FUTURE_NOW_MS test
crates/git/src/tests.rs:LATIN1_SUBJECT test
crates/metrics/src/report_excludes/tests.rs:DROP test # fixture path excluded by the test's own glob; the paired KEEP is the control. No shipped counterpart — the exclude globs users write are config data, never a const here
crates/metrics/src/report_excludes/tests.rs:KEEP test # fixture path the test's exclude glob must NOT match; paired with DROP so a filter that empties a list fails as loudly as one that filters nothing
crates/metrics/src/scores/config.rs:DEFAULT_HIERARCHY_SHARED_DIRS convention -> vocabulary.hierarchySharedDirs # cross-cutting dirs exempted from layering violations
crates/metrics/src/scores/config/feature_sliced_design.rs:DEFAULT_FSD_BASE_DIRS convention -> vocabulary.featureSlicedDesign.baseDirs # FSD base-layer dir name
crates/metrics/src/scores/config/feature_sliced_design.rs:DEFAULT_FSD_ENTRY convention -> vocabulary.featureSlicedDesign.entry # FSD entry-layer dir names; a project using views/screens scores against a layout it never adopted
crates/metrics/src/scores/config/feature_sliced_design.rs:DEFAULT_FSD_SHARED convention -> vocabulary.featureSlicedDesign.shared # FSD shared-layer dir names
crates/metrics/src/scores/config/feature_sliced_design.rs:DEFAULT_FSD_SLICE_CONTAINERS convention -> vocabulary.featureSlicedDesign.sliceContainers # modules is as common as features and would score as cross-slice
crates/metrics/src/scores/config/feature_sliced_design.rs:NEVER internal # unsatisfiable regex used when an FSD layer list is empty; parsing mechanics, not a name
crates/metrics/src/scores/meanings.rs:SCORE_MEANINGS internal # zzop's own disclosure prose — one sentence per score key, shipped as `scoreMeanings` (same axis as `output/timings.rs:MEANING` for the same reason: warning/legend prose is not a name any project picks, and no external spec fixes it). NOT `convention`: the KEYS are zzop's own wire spellings, so nothing here is declarable. The values are the one owner of what each score means — a documentation table would be the second owner, which is exactly why this is a const and not a doc
crates/metrics/src/scores/meanings/tests.rs:POPULATION_FIELD test # the population field each score key must ship (`featureSlicedDesign`->`layerClassifiedImports`, `busFactor`->`total`, ...). `test`, not `convention`: nothing here is declarable and no project picks these names — they are zzop's own wire spellings, asserted against serde's own output so no score can ship a number without the denominator that says what it measured. The other machine-held copy of the same mapping is `crates/metrics/src/health.rs`'s `population_of`; `scores/types/reports/aggregate.rs` stopped restating the field names on 2026-08-14, when its prose copy was found naming `totalImports` for a metric both machines read `layerClassifiedImports` for
crates/summary/src/analyze/adjacent_config.rs:PACK_AXIS_KEYS internal # the three mapped-request keys the envelope lane forwards from an adjacent config. INTERNAL: these are zzop request field names, not a vocabulary about anyone's code — the list exists so the forwarding loop and its doc name one set rather than two
crates/summary/src/analyze/coverage_gaps/census.rs:PRINCIPAL_SHARE_PCT internal # NOT a second threshold: an alias for zzop_facade::MIN_UNCOVERED_EXTENSION_SHARE_PCT, whose declaration site owns the value and its cap rationale. It exists only because this crate must not name zzop-engine directly (see crates/summary/Cargo.toml layering), so the alias hops through the facade re-export. The literal it replaced was a hand-copy, which is exactly the second owner this row must not become again
crates/summary/src/analyze/coverage_gaps/mod.rs:MEANING internal # the run-invariant half of the coverageGaps cell, shipped inside the object it describes so a consumer holding the rows cannot fail to have read what they do and do not prove. Prose, not a threshold — the same discipline cache_signal.rs and the coverage legends follow
crates/summary/src/analyze/tests.rs:CONDITIONAL_UNDER_ITS_OWN_NAME test # which surface-parity carry-conditional rows ride under their own reply key; fail-closed whitelist for a derived pin
crates/summary/src/analyze/tests.rs:EXAMPLE_ENVELOPE test
crates/summary/src/analyze/tests.rs:RUBY_PACK test # fixture: a one-rule pack targeting .rb, the variable whose presence decides whether Mode A judges an unsupported language
crates/summary/src/contracts.rs:CONFIG_TEMPLATE_FILENAME internal
crates/summary/src/contracts.rs:CONFIG_TEMPLATE_NAME internal
crates/summary/src/contracts.rs:DISCLOSURE_CONTRACT_NAME internal # zzop's own contract-document name, not a name a project picks; T1 - the ONE symbol the reply's disclosure pointer and the table row answering it both read, so the pointer cannot rot on one surface while the other stays green
crates/summary/src/contracts.rs:MISSING_CONFIG_MARKER internal # re-export of the config crate's marker (one owner)
crates/summary/src/contracts.rs:URI_PREFIX internal # zzop's own resource URI space (zzop://contract/), not a name a project picks; T1 - moved here from packages/mcp/src/resources.rs, which now imports it, so the MCP resources lane and the reply pointer spell one prefix
crates/summary/src/cross/native_analyses.rs:ZERO_KEY internal # zzop own wire key name for the join reply zero-finding roster list, spelled once so the field and the legend keyed by it cannot drift apart. A NAME zzop defines, not a threshold and not a vocabulary about anyone code — same axis and same reasoning as the graph edge labels below
crates/summary/src/graph/cochange.rs:DEFAULT_COCHANGE_TOP cap # edge cap for the co-change diagram; readability threshold, overridable with --top
crates/summary/src/graph/cosmograph/tests.rs:EMITTER test # workspace-relative path of this lane's emitter, used test-only to locate its owning _cliOnlyLanes entry in surface-parity.json by `sources` rather than by lane name; not a name any project spells
crates/summary/src/graph/dep.rs:DEFAULT_DEP_TOP cap # node cap for the dep diagram; readability threshold, overridable with --top
crates/summary/src/graph/dep/window.rs:HISTORY_COLUMNS internal # the git-derived column names this lane actually emits. Not a tunable but a description of what is emitted, and a column added without adding it here makes the disclosure sentence understate what the window governs
crates/summary/src/graph/mermaid.rs:CLASS_DEFS internal # mermaid classDef names and colours zzop emits
crates/summary/src/graph/mod.rs:DEFAULT_GRAPH_TOP cap
crates/summary/src/graph/model.rs:CONSUME internal # zzop graph edge label
crates/summary/src/graph/model.rs:PROVIDE internal # zzop graph edge label
crates/summary/src/graph/posture.rs:DEFAULT_POSTURE_TOP cap # per-tree route cap for the posture diagram; readability threshold, overridable with --top
crates/summary/src/graph/posture.rs:WRITE_METHODS fact # HTTP method TOKENS fixed by RFC, mirroring http_scan.rs WRITE_HTTP_METHODS - not names a project picks
crates/summary/src/graph/risk.rs:DEFAULT_RISK_TOP cap # per-kind node cap for the risk diagram; readability threshold, overridable with --top
crates/summary/src/graph/vocabulary.rs:WIRE_NAMES internal # zzop's own --domain/--format spellings, not names a project picks
crates/summary/src/manifest/mod.rs:RELATIONS internal # zzop manifest relation names
crates/summary/src/output/bucket_keys.rs:KEY_BUCKETS internal # zzop's own output bucket names
crates/summary/src/output/by_rule_legend.rs:BY_RULE_MEANING internal # the `byRule` map's own disclosure prose (warning prose, per the axis legend, same as `output/timings.rs:MEANING` and `output/cache_signal.rs:MEANING`) — not a name any project picks and no config key reaches it. It states that an entry counts FINDINGS rather than places, so the map is comparable within one rule and not across rules, and it names the two channels (`evidencePaths`, the finding's own `data`) that carry the real cardinality. Deliberately holds NO number and NO rule id: the corpus proved no honest derivation exists at this layer (one `data.sites` key with two shapes across lanes, two fold-shaped keys that are diagnostic context, two folds under names a predicate would have to be told), so a count stamped here would be a guess presented as a fact
crates/summary/src/output/cache_signal.rs:MEANING internal # the cache-provenance line's own disclosure prose (warning prose, per the axis legend, same as `output/timings.rs:MEANING`) — not a name any project picks. It states that a cache hit means REPLAYED rather than recomputed, and that the fingerprint contract covers staleness but not a well-formed hand-edit, which is the whole reason a zero in a warm reply can be audited at all
crates/summary/src/output/deployment_role.rs:ROLE_BUILD_SURFACE internal # see ROLE_SHIPPED. Lowest rank — the product decision this triage moment is really about (build surface sorts below test paths) lives in the module doc beside the predicate, which is where a reviewer can check it against the two arms that feed it
crates/summary/src/output/deployment_role.rs:ROLE_SHIPPED internal # deployment-role RANK, not a name any project spells: zzop defines this ordering and no config key reaches it, so the header question "can another project spell this differently?" answers no. Sits with output/bucket_keys.rs:KEY_BUCKETS and graph/vocabulary.rs:WIRE_NAMES — zzop's own output vocabulary — not with `convention`, whose legend is explicit that those belong in config as a template default. Not `cap` either: a rank is not a bound, and changing it reorders rather than loosens
crates/summary/src/output/deployment_role.rs:ROLE_TEST_PATH internal # see ROLE_SHIPPED. The three ranks are one decision and share one verdict; their VALUES are meaningless alone and carry policy only as an order
crates/summary/src/output/mod.rs:DEFAULT_DEGRADED_LIMIT cap
crates/summary/src/output/mod.rs:DEFAULT_EDGES_LIMIT cap
crates/summary/src/output/mod.rs:DEFAULT_FINDINGS_LIMIT cap
crates/summary/src/output/mod.rs:MAX_LIMIT cap
crates/summary/src/output/rule_prose/cost.rs:REF_FIELD_BYTES internal # what one ADDED json field costs on the wire beyond its own value: the quoted name, the `: `, the `,` joining it to the next field, and the newline plus indent a pretty printer charges at the depth a finding's fields sit. `internal`, NOT `fact` -- and this row said `fact` until 2026-08-31. Only the `: ` + `,` + newline is the serializer's: the NAME is `rule_prose/mod.rs:MESSAGE_REF`, censused `internal` two rows below, and the INDENT is zzop's own envelope DEPTH. No framework fixed either half, and `VERSIONING.md` lets a version bump rename a wire field -- so the axis this row claimed was read off the smallest term in the sum. The name term is now READ from that constant instead of being spelled `12`, so the arithmetic follows a rename rather than silently mispricing the fold. NOT `cap`: nothing is bounded by it, and raising it would not loosen a limit, it would make the fold's arithmetic wrong in the direction that folds things which cost bytes. Deliberately the PRETTY figure although two serializers ship (analyze/cross pretty-print, the envelope lane is compact), because the gate must answer "does this pay in the most expensive one?" — a fold that pays there pays by strictly more in the other
crates/summary/src/output/rule_prose/cost.rs:TABLE_ROW_BYTES internal # the same accounting for one `ruleMessages` row, which sits one level shallower than a finding's fields. Same axis and same argument as REF_FIELD_BYTES above, and it moved off `fact` with it on 2026-08-31: no name term here (a row's key IS its data), but the depth is zzop's own envelope shape and not something a serializer fixed; a separate constant rather than a shared one because the DEPTH differs, and one value would have been right for a finding's field and silently wrong for a table row
crates/summary/src/output/rule_prose/mod.rs:MESSAGE_REF internal # zzop's own wire spelling for the per-finding key that points at a folded message's entry in `ruleMessages` — the same axis as `output/bucket_keys.rs:KEY_BUCKETS` and `graph/vocabulary.rs:WIRE_NAMES`, and for the same reason: no project picks this name and no config key reaches it. NOT `convention`. NOT `fact` either — no framework fixed it; `VERSIONING.md` did, which is what makes it zzop's to define and zzop's to keep stable. It is a const rather than a literal because two languages read it: the Rust producer writes it here and `scripts/measure/resolve-folded-message.mjs` resolves through it, and the seam pin in `wire_contract_tests` checks the JS spelling against THIS constant so the pair cannot drift
crates/summary/src/output/rule_prose/mod.rs:RULE_MESSAGES_MEANING internal # the `ruleMessages` table's own disclosure prose (warning/legend prose per the axis legend, exactly as `output/by_rule_legend.rs:BY_RULE_MEANING` and `output/cache_signal.rs:MEANING`) — not a name any project picks, no config key reaches it. It states the one thing a reader cannot infer from the table: that the stored text is BYTE-IDENTICAL to what the finding used to carry inline, that resolution goes through the `messageRef` FIELD rather than by parsing the pointer sentence, and that this is deduplication and never truncation (`truncated` keeps that meaning alone). It rides once per reply precisely so the per-finding pointer can stay an address instead of restating this on every folded finding — which is the byte argument the fold exists for, applied to itself
crates/summary/src/output/tests.rs:LONG_PROSE test # the shared long message every fold fixture folds. A fixture INPUT, not policy — but its LENGTH is load-bearing in a way most fixtures' are not: the fold is gated on net bytes, so a short fixture is left inline and every pin written about key assignment or reconstructibility would pass by never folding at all. Sized from the measured median shipped rule message rather than raised until the tests went green, which is the difference between a representative fixture and one tuned to clear a threshold
crates/summary/src/output/timings.rs:MEANING internal # the rule-timing report's own disclosure prose (warning prose, per the axis legend) — not a name any project picks; it states that cache hits contribute no timing, which is the whole reason a short warm-run report cannot be misread as a fast one
crates/summary/src/siblings.rs:MAX_NAMED_SIBLINGS cap
crates/summary/src/suggest.rs:BUCKETS internal # zzop's own suggestion bucket names
crates/summary/src/suggest.rs:SUGGESTIONS_LIMIT cap
crates/summary/src/wire_contract_tests.rs:INDEX_ANCHOR test # the purpose-named attribute this pin slices site/reference.html's field index at. A NEEDLE into published markup, not a tunable — and it is asserted-on rather than trusted: a page that stops carrying it panics naming the heading, because a needle that silently matches nothing turns the whole check vacuously green
crates/test-support/src/lib.rs:SITE_MARKER_SUFFIX test # the marker identifier skip_notice! plants in a test body so the enclosing test can be named; no user-visible policy
parser/parser-csharp/src/adapters/ef_core.rs:EF_CORE_SPECIFIERS fact # Microsoft.EntityFrameworkCore - the published EF Core namespace the DbSet gate anchors on
parser/parser-csharp/src/adapters/ef_core.rs:TABLE_ATTRIBUTE_SPECIFIERS fact # System.ComponentModel.DataAnnotations.Schema - the published namespace [Table] lives in
parser/parser-csharp/src/adapters/ef_core/tests.rs:EF_USING test
parser/parser-csharp/src/adapters/ef_core/tests.rs:SCHEMA_USING test
parser/parser-csharp/src/adapters/http_clients.rs:HTTP_CLIENT_SPECIFIERS fact # .NET namespaces
parser/parser-csharp/src/adapters/http_clients.rs:VERB_METHODS fact # read the decl - GetAsync/PostAsJsonAsync are System.Net.Http.HttpClient's own method names
parser/parser-csharp/src/adapters/http_clients/tests.rs:IMPORT test
parser/parser-csharp/src/adapters/provides/attribute_controller.rs:METHOD_ATTRIBUTES fact # ASP.NET Core attribute names
parser/parser-csharp/src/adapters/provides/minimal_api.rs:MAP_METHODS fact # ASP.NET Minimal API MapGet/MapPost builder methods
parser/parser-csharp/src/lang/call_sites.rs:CONSOLE_WRITER_PROPERTIES fact # System.Console's own Error/Out writer-property names — platform-fixed; the property rides in the callee verbatim rather than in a folded stream field (the channel's false-fold rule)
parser/parser-csharp/src/lang/call_sites.rs:CONSOLE_WRITE_METHODS fact # System.Console's own Write/WriteLine method names — platform-fixed, not a project's. The scope choice (the two that WRITE a message, not ReadLine/Clear) is the producer's judgment and is argued at the declaration
parser/parser-csharp/src/lang/call_sites.rs:HASH_ALGORITHM_TYPES fact # System.Security.Cryptography digest class names (MD5/SHA1/SHA256/...) — .NET own type names, where the TYPE is the algorithm; HMAC and cipher types deliberately absent, argued at the declaration
parser/parser-csharp/src/lang/call_sites.rs:HASH_FACTORY_METHOD fact # the Create static factory every .NET digest type exposes — platform-fixed
parser/parser-csharp/src/lang/call_sites.rs:HASH_GENERIC_FACTORY_TYPES fact # HashAlgorithm/CryptoConfig — the .NET factories whose algorithm is a string ARGUMENT rather than the type name, i.e. the one C# spelling with a never-guess case
parser/parser-csharp/src/lang/call_sites.rs:PROCESS_START fact # System.Diagnostics.Process own static launcher name — .NET platform API, matched as the chain tail so the optional namespace prefix is not a different fact
parser/parser-csharp/src/lang/call_sites.rs:PROCESS_START_INFO_TYPE fact # System.Diagnostics.ProcessStartInfo — the .NET type whose construction configures a launch; recognized in its new form only, judgment argued at the declaration
parser/parser-csharp/src/lang/loop_spans.rs:LOOP_STATEMENT_KINDS fact # tree-sitter node kinds
parser/parser-csharp/src/lang/used_names.rs:DECLARES_NAME_FIELD fact # tree-sitter node kinds
parser/parser-csharp/src/lib.rs:FRAMEWORK_RECOGNIZERS fact # which frameworks this parser recognizes, named as their OWN ecosystems spell them and keyed by the extensions those ecosystems use — fixed externally, not a knob. The row set is bound to the adapter modules on disk by rule_contracts::recognizer_drift, so it cannot drift into a claim about a recognizer that does not ship
parser/parser-csharp/src/lib.rs:PARSER_FINGERPRINT internal
parser/parser-csharp/src/lib.rs:TOP_LEVEL_DECLARATION_KINDS fact # tree-sitter node kinds
parser/parser-csharp/src/node_kinds.rs:PINNED_ANONYMOUS_KEYWORDS fact # C# keywords
parser/parser-csharp/src/node_kinds.rs:PINNED_NODE_KINDS fact # tree-sitter node kinds
parser/parser-go/src/adapters/gin.rs:GIN_VERB_METHODS fact # aliases HTTP_KEY_VERBS - gin's router methods are the verb names
parser/parser-go/src/adapters/gin/cross_file.rs:GIN_RECEIVER_TYPES fact # gin's own exported type names
parser/parser-go/src/adapters/gorm.rs:QUERY_METHODS fact # read the decl - Model/Where/Preload/FirstOrCreate are GORM's own chainable API; the decl doc reasons about gorm's API surface explicitly
parser/parser-go/src/adapters/gorm/tests.rs:IMPORT test
parser/parser-go/src/adapters/http_clients.rs:VERB_METHODS fact # net/http client method names
parser/parser-go/src/adapters/net_http.rs:VERB_METHODS fact # HandleFunc/Handle are net/http's own registration functions
parser/parser-go/src/lang/call_sites.rs:ENV_READ_CALLEES fact # Go stdlib API spellings (os.Getenv / os.LookupEnv) — the language's own names, same axis as the Python producer's ENV_READ_CALLEES
parser/parser-go/src/lang/call_sites.rs:FMT_FPRINT_METHODS fact # fmt's own Fprint/Fprintln/Fprintf names — console-write only when the writer is spelled os.Stdout/os.Stderr at the site (STD_STREAM_SPELLINGS)
parser/parser-go/src/lang/call_sites.rs:FMT_PRINT_METHODS fact # fmt's own Print/Println/Printf names — the stdout-writing trio; Sprint*/Errorf build strings and are deliberately out (the decl's module doc)
parser/parser-go/src/lang/call_sites.rs:HASH_CONSTRUCTORS fact # crypto/* package entry points that construct or compute a digest (New/Sum...) — Go stdlib own names
parser/parser-go/src/lang/call_sites.rs:HASH_PACKAGES fact # crypto/* stdlib digest package names — the PACKAGE is the algorithm in Go, so this list is both the family gate and the algorithm vocabulary; scope argued at the declaration
parser/parser-go/src/lang/call_sites.rs:PROCESS_EXEC_CALLEES fact # os/exec own two constructors (exec.Command / exec.CommandContext) — Go stdlib names; the Cmd builder methods are deliberately absent (one construction is one process)
parser/parser-go/src/lang/call_sites.rs:STD_STREAM_SPELLINGS fact # os.Stdout / os.Stderr — the two first-argument spellings that make an Fprint* call a console write; a spelling test, never a data-flow proof
parser/parser-go/src/lang/resolve/tests.rs:MODULE test
parser/parser-go/src/lang/used_names.rs:DECLARES_NAME_FIELD fact # tree-sitter node kinds
parser/parser-go/src/lib.rs:FRAMEWORK_RECOGNIZERS fact # which frameworks this parser recognizes, named as their OWN ecosystems spell them and keyed by the extensions those ecosystems use — fixed externally, not a knob. The row set is bound to the adapter modules on disk by rule_contracts::recognizer_drift, so it cannot drift into a claim about a recognizer that does not ship
parser/parser-go/src/lib.rs:PARSER_FINGERPRINT internal
parser/parser-go/src/lib.rs:TOP_LEVEL_DECLARATION_KINDS fact # tree-sitter node kinds
parser/parser-go/src/node_kinds.rs:PINNED_NODE_KINDS fact # tree-sitter node kinds
parser/parser-java-21/src/http_clients.rs:RESTTEMPLATE_SPECIFIERS fact # Spring's own RestTemplate FQN + its package glob - the import gate
parser/parser-java-21/src/http_clients.rs:RESTTEMPLATE_VERB_METHODS fact # read the decl - getForObject/postForEntity are RestTemplate's own client-specific method names (put/delete deliberately absent; the decl doc reasons why)
parser/parser-java-21/src/http_clients.rs:WEBCLIENT_SPECIFIERS fact # Spring's own WebClient FQN + its package glob - the import gate
parser/parser-java-21/src/http_clients/tests.rs:RT_IMPORT test
parser/parser-java-21/src/http_clients/tests.rs:WC_IMPORT test
parser/parser-java-21/src/jpa.rs:JPA_PACKAGE_PREFIXES fact # jakarta.persistence/javax.persistence - the JPA spec's own annotation packages
parser/parser-java-21/src/jpa/tests.rs:IMPORTS test
parser/parser-java-21/src/lang/call_sites.rs:PRINT_STREAM_WRITE_METHODS fact # java.io.PrintStream's own print/println/printf/format names — platform-fixed; the scope choice (the four that WRITE a message, not append/write/flush) is the producer's judgment and is argued at the declaration
parser/parser-java-21/src/lang/call_sites.rs:PROCESS_BUILDER_TYPE fact # java.lang.ProcessBuilder — the JDK type whose construction launches a process; recognized in its new form only, constructor judgment argued at the module doc
parser/parser-java-21/src/lang/call_sites.rs:SYSTEM_STREAM_FIELDS fact # java.lang.System's own out/err stream-field names — the field rides in the callee verbatim rather than in a folded stream field (the channel's false-fold rule)
parser/parser-java-21/src/lang/call_sites/hash_call.rs:MESSAGE_DIGEST_TYPE fact # java.security.MessageDigest's own type name, fixed by the JDK
parser/parser-java-21/src/lang/loop_spans.rs:LOOP_STATEMENT_KINDS fact # tree-sitter node kinds
parser/parser-java-21/src/lang/symbols/member.rs:INSTANCE_BLOCK internal # the member name zzop gives a Java instance initializer, which the language leaves unnamed — STATIC_BLOCK's twin, same axis and same reasoning
parser/parser-java-21/src/lang/symbols/member.rs:STATIC_BLOCK internal # the member name zzop gives a Java `static { … }` initializer, which the language leaves unnamed. Same axis as parser-typescript's STATIC_BLOCK and deliberately the same spelling: it is not a name any project writes or could declare in config, it is zzop's own symbol spelling and half of the `{file}#Class.static-block` id its own consumers read. The `-` is load-bearing (no Java identifier contains one), which is what makes it collision-proof against a real member name
parser/parser-java-21/src/lang/used_names.rs:DECLARES_NAME_FIELD fact # tree-sitter node kinds
parser/parser-java-21/src/lib.rs:FRAMEWORK_RECOGNIZERS fact # which frameworks this parser recognizes, named as their OWN ecosystems spell them and keyed by the extensions those ecosystems use — fixed externally, not a knob. The row set is bound to the adapter modules on disk by rule_contracts::recognizer_drift, so it cannot drift into a claim about a recognizer that does not ship
parser/parser-java-21/src/lib.rs:PARSER_FINGERPRINT internal
parser/parser-java-21/src/lib.rs:TOP_LEVEL_DECLARATION_KINDS fact # tree-sitter node kinds
parser/parser-java-21/src/node_kinds.rs:PINNED_ANONYMOUS_KEYWORDS fact # Java keywords
parser/parser-java-21/src/node_kinds.rs:PINNED_NODE_KINDS fact # tree-sitter node kinds
parser/parser-java-21/src/provides/annotations.rs:METHOD_ANNOTATIONS fact # Spring's @GetMapping family - the canonical example in the vocabulary decision
parser/parser-java-21/src/provides/annotations.rs:REQUEST_METHOD_NAMES fact # Spring's RequestMethod enum constants
parser/parser-java-21/src/security.rs:SECURITY_ANNOTATIONS fact # Spring Security / JSR-250 annotation names
parser/parser-java-21/src/spring_security/clauses.rs:AUTHZ_ENTRYPOINTS fact # Spring Security DSL method names
parser/parser-java-21/src/spring_security/fold.rs:AUTHENTICATED_MANAGER fact # Spring Security class name — the one AuthorizationManager whose identity proves an authenticated-by-default anyRequest terminal
parser/parser-java-21/src/spring_security/fold.rs:CLOSED_TERMINALS fact # Spring Security DSL method names
parser/parser-java-21/src/spring_security/fold.rs:MATCHER_METHODS fact # Spring Security DSL method names
parser/parser-java-21/src/spring_security/tests.rs:BE_SPRING test
parser/parser-prisma/src/analysis.rs:DEFAULT_PRISMA_CLIENT_GETTER_FN convention -> vocabulary.prismaClientGetter # getPrisma is a name each project invents for its client accessor; the DEFAULT_ prefix already concedes it
parser/parser-prisma/src/lib.rs:FRAMEWORK_RECOGNIZERS fact # which frameworks this parser recognizes, named as their OWN ecosystems spell them and keyed by the extensions those ecosystems use — fixed externally, not a knob. The row set is bound to the adapter modules on disk by rule_contracts::recognizer_drift, so it cannot drift into a claim about a recognizer that does not ship
parser/parser-prisma/src/lib.rs:PARSER_FINGERPRINT internal
parser/parser-prisma/src/tests.rs:ENUM_SAMPLE test
parser/parser-prisma/src/tests.rs:SAMPLE test
parser/parser-python-3/src/adapters/django/tests.rs:IMPORT test
parser/parser-python-3/src/adapters/fastapi.rs:VERB_DECORATORS fact # FastAPI's own @app.get/@app.post decorator names
parser/parser-python-3/src/adapters/fastapi/guard/depends.rs:ANONYMOUS_SWITCH_KEYWORDS fact # auto_error/required are fastapi.security constructor kwargs
parser/parser-python-3/src/adapters/fastapi/guard/depends.rs:SECURITY_SCHEME_CLASSES fact # fastapi.security class names
parser/parser-python-3/src/adapters/guard_vocab.rs:ANONYMOUS_VETO_SUBSTRINGS convention -> vocabulary.pythonGuardAnonymousVetoSubstrings # substrings of PROJECT-authored dependency names (optional, maybe, anonymous)
parser/parser-python-3/src/adapters/guard_vocab.rs:GUARD_SUBSTRINGS convention -> vocabulary.pythonGuardSubstrings # how a project spells its own auth dependency functions - the Python twin of DEFAULT_AUTH_GUARD_PATTERN
parser/parser-python-3/src/adapters/guard_vocab.rs:REPORT_VETO_PREFIXES convention -> vocabulary.pythonGuardReportVetoPrefixes # verb heads of project-authored function names
parser/parser-python-3/src/adapters/guard_vocab.rs:REPORT_VETO_SUFFIXES convention -> vocabulary.pythonGuardReportVetoSuffixes # noun tails of project-authored function names
parser/parser-python-3/src/adapters/http_clients.rs:VERB_METHODS fact # requests/httpx module-level verb functions
parser/parser-python-3/src/adapters/http_clients/instances.rs:CLIENT_CTORS fact # requests.Session / httpx.Client / httpx.AsyncClient class names
parser/parser-python-3/src/adapters/sqlalchemy.rs:FIRST_ARG_VERBS fact # SQLAlchemy API function names
parser/parser-python-3/src/adapters/sqlalchemy.rs:MULTI_MODEL_VERBS fact # SQLAlchemy API function names
parser/parser-python-3/src/adapters/sqlalchemy/tests.rs:IMPORT test
parser/parser-python-3/src/lang/call_sites.rs:BUILTIN_PRINT fact # the name of a Python BUILT-IN — fixed by the language, not a project convention. A project that shadows it makes the file silent rather than making this name declarable (see the module's rebinding note)
parser/parser-python-3/src/lang/call_sites.rs:ENV_READ_CALLEES fact # Python stdlib API spellings (os.getenv / os.environ.get) — the language's own names, same axis as VERB_METHODS and FIRST_ARG_VERBS above
parser/parser-python-3/src/lang/call_sites.rs:ENV_READ_MAPPING fact # the stdlib mapping os.environ, read through a subscript — same axis as ENV_READ_CALLEES, split out because the node shape differs
parser/parser-python-3/src/lang/call_sites.rs:PROCESS_EXEC_CALLEES fact # Python stdlib process API spellings (subprocess run/call/check_call/check_output/Popen, os.system, os.popen) — the language own names, same axis as ENV_READ_CALLEES above
parser/parser-python-3/src/lang/call_sites/hash_call.rs:HASHLIB_CONSTRUCTORS fact # hashlib's own constructor names, fixed by the Python stdlib
parser/parser-python-3/src/lang/call_sites/hash_call.rs:HASHLIB_NEW fact # hashlib.new's own spelling, fixed by the Python stdlib
parser/parser-python-3/src/lib.rs:FRAMEWORK_RECOGNIZERS fact # which frameworks this parser recognizes, named as their OWN ecosystems spell them and keyed by the extensions those ecosystems use — fixed externally, not a knob. The row set is bound to the adapter modules on disk by rule_contracts::recognizer_drift, so it cannot drift into a claim about a recognizer that does not ship
parser/parser-python-3/src/lib.rs:PARSER_FINGERPRINT internal
parser/parser-rust/src/adapters/axum.rs:VERB_METHODS fact # axum::routing's own get/post handler constructors
parser/parser-rust/src/adapters/axum/tests.rs:OUT_OF_REACH_ROUTERS test # router shapes this adapter never reaches (impl/trait/nested mod), pinned so the doc's claim that it needs only the Item axis stays measured
parser/parser-rust/src/adapters/axum/tests.rs:SHIPPED_REL test # the one shipped path the attribute-gate fixtures sit at, so none of them can pass via the path gate instead
parser/parser-rust/src/adapters/axum/tests.rs:TEST_GATED_ROUTERS test # the test-gated source shapes whose axum routes must not be minted as deployed provides
parser/parser-rust/src/adapters/http_clients.rs:VERB_METHODS fact # reqwest Client's own verb methods
parser/parser-rust/src/adapters/http_clients/tests.rs:SHIPPED_REL test # the one shipped path the attribute-gate fixtures sit at, so none of them can pass via the path gate instead
parser/parser-rust/src/adapters/http_clients/tests.rs:TEST_GATED_EGRESS test # the test-gated source shapes whose reqwest egress must not be minted as deployed
parser/parser-rust/src/adapters/raw_sql.rs:MAX_PLACEHOLDER_BODY cap # longest format-spec body treated as a placeholder rather than prose; a bound on zzop's own masking pass, not a project knob
parser/parser-rust/src/adapters/raw_sql.rs:PLACEHOLDER internal # zzop's own template-hole token — the same spelling parser-typescript/src/adapters/raw_sql.rs uses
parser/parser-rust/src/adapters/raw_sql/tests.rs:FIXTURE test # a `#[cfg(test)] mod` fixture query inside this adapter's own test source
parser/parser-rust/src/adapters/raw_sql/tests.rs:TEST_GATED_SQL_FIXTURES test # the test-gated source shapes whose SQL must not be minted as deployed db coupling
parser/parser-rust/src/lang/call_sites.rs:ENV_READ_CALLEES fact # Rust stdlib path spellings (std::env::var / var_os, both the fully qualified and the use-qualified form) — the language's own names; vars/vars_os (whole-environment iteration) deliberately absent, and console-write is deliberately not produced at all (the println! judgment, argued in the decl's module doc)
parser/parser-rust/src/lang/call_sites.rs:HASH_CALLEES fact # the (spelling, algorithm) pairs of the digest crates Rust actually hashes with — the ONE place a sibling third-party surface is claimed, because Rust std ships no digest and the platform-API rule would leave the family empty; argued at the declaration and in the module doc
parser/parser-rust/src/lang/call_sites.rs:PROCESS_EXEC_CALLEES fact # std::process::Command::new in each of its three spellings — the platform own path, kept per spelling for the same original-spelling reason ENV_READ_CALLEES is
parser/parser-rust/src/lang/extractor_guards.rs:RUST_OPTIONAL_EXTRACTOR_PREFIXES convention -> vocabulary.rustOptionalExtractorPrefixes # how a project spells its own OPTIONAL request extractor (MaybeAuthUser, OptionalUser) - the Rust twin of pythonGuardAnonymousVetoSubstrings
parser/parser-rust/src/lang/resolve.rs:PATH_ATTR_HEAD internal # zzop's own encoding for "this mod declaration carries #[path]", chosen non-identifier-shaped (leading #) so it cannot collide with a real Rust specifier; a project cannot rename it and no external spec fixes it. Same axis and same reasoning as the CALL_KIND_* wire spellings — it is a description of OUR wiring. Two engine sites read it as a SYMBOL rather than re-spelling it (assemble::helpers::rust, assemble::collect::candidates), which is the T1 answer to the drift this census exists to notice
parser/parser-rust/src/lang/resolve.rs:PATH_ATTR_PREFIX internal # PATH_ATTR_HEAD plus its separator, defined beside it so the head and the separator cannot drift apart; pinned to its head by lang::resolve::tests
parser/parser-rust/src/lib.rs:FRAMEWORK_RECOGNIZERS fact # which frameworks this parser recognizes, named as their OWN ecosystems spell them and keyed by the extensions those ecosystems use — fixed externally, not a knob. The row set is bound to the adapter modules on disk by rule_contracts::recognizer_drift, so it cannot drift into a claim about a recognizer that does not ship
parser/parser-rust/src/lib.rs:PARSER_FINGERPRINT internal
parser/parser-sql/src/extract.rs:SEGMENT fact # SQL identifier-quoting syntax fixed by the dialects
parser/parser-sql/src/lib.rs:FRAMEWORK_RECOGNIZERS fact # which frameworks this parser recognizes, named as their OWN ecosystems spell them and keyed by the extensions those ecosystems use — fixed externally, not a knob. The row set is bound to the adapter modules on disk by rule_contracts::recognizer_drift, so it cannot drift into a claim about a recognizer that does not ship
parser/parser-sql/src/lib.rs:PARSER_FINGERPRINT internal
parser/parser-typescript/src/adapters/client_base.rs:CLIENT_BASE_PREFIX_KIND internal # zzop sentinel kind
parser/parser-typescript/src/adapters/controller_decorators/context.rs:CONTROLLER_CLASS_GATES fact # @Controller/@RestController decorator names fixed by Nest/Spring
parser/parser-typescript/src/adapters/controller_decorators/method_facts.rs:METHOD_DECORATORS fact # Nest's @Get/@Post decorator names
parser/parser-typescript/src/adapters/db_table_consume.rs:PRISMA_CLIENT_GETTER convention -> vocabulary.prismaClientGetter # getPrisma is a project-invented accessor name (sibling of parser-prisma's DEFAULT_PRISMA_CLIENT_GETTER_FN)
parser/parser-typescript/src/adapters/db_table_consume.rs:QUERY_METHODS fact # read the decl - findMany/findFirst/findUnique/count are Prisma Client's own delegate methods
parser/parser-typescript/src/adapters/egress/body_shape.rs:BODY_POSITION_VERBS fact # HTTP method tokens
parser/parser-typescript/src/adapters/egress/react_query.rs:QUERY_HOOKS fact # TanStack Query's exported hook names
parser/parser-typescript/src/adapters/egress/react_query.rs:RQ test
parser/parser-typescript/src/adapters/egress/retry.rs:RETRY_WRAPPERS convention -> vocabulary.retryWrappers # AMBIGUOUS - these are the libraries' documented default export names, but they are default IMPORT BINDINGS a consumer renames at will
parser/parser-typescript/src/adapters/egress/retry.rs:RETRY_WRAPPER_PACKAGES fact # published npm package names
parser/parser-typescript/src/adapters/egress/retry.rs:RETRY_WRITE_VERBS fact # HTTP method TOKENS fixed by RFC, not names a project picks. T2 - pinned equal to rules-http WRITE_HTTP_METHODS in engine policy_pins.rs (parsers may not depend on rule crates)
parser/parser-typescript/src/adapters/global_prefix.rs:NEST_GLOBAL_PREFIX_KIND internal # zzop sentinel kind
parser/parser-typescript/src/adapters/nest_middleware/tests.rs:BE_NEST_SHAPE test
parser/parser-typescript/src/adapters/pathname_dispatch.rs:SERVICE_WORKER_LIFECYCLE_MARKERS fact # browser service-worker lifecycle APIs, fixed by the ServiceWorker spec — the only markers that separate a service worker from a legacy-syntax Cloudflare Worker, which shares the addEventListener("fetch") entry point this adapter targets. Widening this list vetoes real servers, so it is spec vocabulary rather than a tunable
parser/parser-typescript/src/adapters/raw_sql.rs:PLACEHOLDER internal # zzop's own template-hole token
parser/parser-typescript/src/adapters/router_mounts/guard.rs:AUTH_GUARDED_ATTR_KEY internal # zzop producer/consumer attribute key
parser/parser-typescript/src/adapters/router_mounts/guard.rs:ENV_AXIS_VETO_SUBSTRINGS convention -> vocabulary.envAxisVetoSubstrings # substrings of project-authored middleware names
parser/parser-typescript/src/adapters/router_mounts/guard.rs:MIDDLEWARE_GUARD_CALLEES convention -> vocabulary.middlewareGuardCallees # AMBIGUOUS - passport.authenticate/clerkMiddleware are library API, but requiresAuth/ensureLoggedIn/checkJwt are names a project writes itself
parser/parser-typescript/src/adapters/router_mounts/guard.rs:ROUTER_NAME_VETO_SUFFIXES convention -> vocabulary.routerNameVetoSuffixes # suffixes of project-authored router/service/module identifiers
parser/parser-typescript/src/adapters/router_mounts/guard.rs:WRAPPER_GUARD_PREFIXES convention -> vocabulary.wrapperGuardPrefixes # require/ensure/protect/restrict are prefixes of project-authored wrapper functions
parser/parser-typescript/src/adapters/router_mounts/idempotency.rs:IDEMPOTENCY_GUARDED_ATTR_KEY internal # zzop producer/consumer attribute key
parser/parser-typescript/src/adapters/router_mounts/idempotency.rs:IDEMPOTENCY_HEADER_NAMES convention -> vocabulary.idempotencyHeaderNames # AMBIGUOUS - Idempotency-Key is an IETF draft header, but the x- variant and any house header name are the project's choice
parser/parser-typescript/src/call_sites.rs:CONSOLE_WRITE_METHODS fact # WHATWG console method names — the platform's, not a project's. The scope choice (the six that WRITE, not table/dir/group/time) is the producer's judgment and is argued at the declaration
parser/parser-typescript/src/call_sites/hash_call.rs:CRYPTO_HASH_METHODS fact # Node crypto digest constructor names (createHash/createHmac) — the platform API; createCipheriv and WebCrypto subtle deliberately absent
parser/parser-typescript/src/call_sites/hash_call.rs:CRYPTO_SPECIFIERS fact # the two module specifiers that ARE Node crypto (bare and node:-prefixed) — Node own resolution rules fix this equivalence
parser/parser-typescript/src/call_sites/process_exec.rs:CHILD_PROCESS_EXEC_METHODS fact # Node child_process own function names (exec/execSync/spawn/spawnSync/execFile/execFileSync) — the platform API, not a project vocabulary; fork deliberately absent (different failure mode)
parser/parser-typescript/src/call_sites/process_exec.rs:CHILD_PROCESS_SPECIFIERS fact # the two module specifiers that ARE Node child-process module (bare and node:-prefixed) — Node own resolution rules fix this equivalence
parser/parser-typescript/src/framework_recognizers.rs:FRAMEWORK_RECOGNIZERS fact # which frameworks this parser recognizes, named as their OWN ecosystems spell them and keyed by the extensions those ecosystems use — fixed externally, not a knob. The row set is bound to the adapter modules on disk by rule_contracts::recognizer_drift, so it cannot drift into a claim about a recognizer that does not ship
parser/parser-typescript/src/lang/resolve/framework_alias.rs:NUXT_CONFIG_FILES fact # the filenames Nuxt itself looks for to locate an app root -- fixed by the framework, not by us. THE one owner since 2026-08-31: `zzop_engine`'s Nuxt auto-import assembly carried a byte-identical hand copy across the crate boundary (its own doc ASSERTED the equality and nothing enforced it) and now `use`s this slice instead -- T1, available all along because the engine already depends on this crate
parser/parser-typescript/src/lang/resolve/framework_alias.rs:NUXT_PREFIXES fact # the alias spellings Nuxt reserves (~ ~~ @ @@); a project cannot rename them, so there is nothing here to configure
parser/parser-typescript/src/lang/resolve/framework_alias.rs:SVELTE_CONFIG_FILES fact # the filenames SvelteKit itself looks for to locate an app root -- fixed by the framework, not by us; the sibling of NUXT_CONFIG_FILES and read by the same walk
parser/parser-typescript/src/lang/resolve/specifier.rs:EXTENSION_FALLBACKS fact # NodeNext .js -> .ts mapping fixed by TypeScript/Node resolution
parser/parser-typescript/src/lang/resolve/specifier.rs:RESOLVE_EXTS fact # TS/JS extensions fixed by the language and its resolver
parser/parser-typescript/src/lang/write_site.rs:ATOMIC_OP_KEYS fact # Prisma atomic-update operation keys
parser/parser-typescript/src/lang/write_site.rs:COUNTER_METHODS fact # Redis command names (incr/incrby/decr/decrby)
parser/parser-typescript/src/lang/write_site.rs:CREATE_METHODS fact # Prisma/TypeORM/Knex API method names
parser/parser-typescript/src/lang/write_site.rs:DEFAULT_ORM_RECEIVER_PATTERN convention -> vocabulary.ormReceiverPattern # Repository/Store suffixes and bare db/tx/orm handles are names a project gives its own ORM receivers; the DEFAULT_ prefix already concedes it
parser/parser-typescript/src/lang/write_site.rs:DEFAULT_WRITE_METHODS convention -> vocabulary.ormWriteMethods # AMBIGUOUS - read the decl - most entries are Prisma/TypeORM API, but it backs a generic "any write" check, so a hand-rolled repository spelling writes persist/store/commit is silently missed
parser/parser-typescript/src/lang/write_site.rs:UPDATE_METHODS fact # Prisma/TypeORM API method names
parser/parser-typescript/src/lang/write_site.rs:WRITE_LABEL_TOKEN_COUNT cap
parser/parser-typescript/src/lib.rs:ARRAY_ITERATION_METHODS fact # ECMAScript Array.prototype method names
parser/parser-typescript/src/lib.rs:PARSER_FINGERPRINT internal
parser/parser-typescript/src/lib.rs:PROMISE_CONTINUATION_METHODS fact # ECMAScript Promise method names
parser/parser-typescript/src/prescan.rs:BARE_ESM_MAX_STATEMENT_LINES cap # how many lines one bare-ESM import statement may span before the slicer discards it as prose -- a bound we chose (measured: the two multi-line statements in the dogfood corpus span 4 lines each), not a name any framework or project owns
parser/parser-typescript/src/prescan.rs:PRESCAN_IMPORT_HOSTS fact # which filetypes carry imports a lexical pre-scan can read, and WHERE each one puts them — a pair table rather than a roster, because the answer differs per dialect and one list answering two questions is what this table replaced. Fixed by the frameworks (Vue/Svelte SFC script blocks, VitePress/Nuxt Content markdown, MDX top-level ESM, Astro frontmatter fences), never named by a project. Same axis and same reason as NUXT_CONFIG_FILES
parser/parser-typescript/src/sfc_imports.rs:SFC_SCRIPT_HOST_EXTENSIONS fact # which filetypes a framework compiles into a Vue SFC is fixed by the framework (Vue/Svelte SFCs, VitePress and Nuxt Content markdown pages), not named by any project. Its membership is bounded by the EXTRACTOR rather than by taste: an entry qualifies only if the dialect puts its imports inside a <script> tag, which is why .mdx and .astro are absent HERE -- they are pre-scanned through the other two arms of PRESCAN_IMPORT_HOSTS, which is the wider roster this one is the ScriptBlocks third of. Not the same question as NON_SOURCE_EXTENSIONS -- .md is on both lists and .vue on neither, and dispatch/non_source.rs owns why
parser/parser-typescript/src/symbol_shapes/class.rs:ACCESSOR_SET internal # the trailing segment a class SETTER takes when a same-name getter holds the plain spelling (`C.x` / `C.x.set`). Same axis as STATIC_BLOCK beside it and for the same reason: not a name any project writes or could declare in config, it is zzop own symbol spelling and part of the `{file}#Class.member.set` id its own consumers read. Unlike STATIC_BLOCK it is NOT collision-proof by construction — the choice of a trailing `set` over the middle `C.set.x` is what keeps the one reachable collision out of legal TypeScript, and the declaration doc carries that measurement
parser/parser-typescript/src/symbol_shapes/class/members.rs:STATIC_BLOCK internal # the member name zzop gives a class static initialization block, which the language leaves unnamed. NOT `convention`: it is not a name any project writes or could declare in config — it is zzop's own symbol spelling, half of the `{file}#Class.static-block` id its own consumers read, the same axis as the CALL_KIND_*/sentinel-kind spellings above. The `-` is load-bearing (no JS identifier contains one), which is what makes it collision-proof against a real member name
rules/native/rules-cross-layer/src/cross_layer/all_consumes_unjoined.rs:MAX_SAMPLE_KEYS cap
rules/native/rules-cross-layer/src/cross_layer/all_consumes_unjoined.rs:MIN_UNJOINED_CONSUMES cap
rules/native/rules-cross-layer/src/cross_layer/all_consumes_unjoined/subsume.rs:REPLACED internal # zzop rule ids this fold replaces, not user vocabulary
rules/native/rules-cross-layer/src/cross_layer/ambiguous_consume.rs:MAX_CANDIDATES cap
rules/native/rules-cross-layer/src/cross_layer/blindness.rs:MIN_TOTAL_CONSUMES cap
rules/native/rules-cross-layer/src/cross_layer/external_host_fanout.rs:FANOUT_MIN_FILES cap
rules/native/rules-cross-layer/src/cross_layer/external_secret_in_url.rs:SECRET_PARAM_NAMES convention -> vocabulary.secretParamNames # query-parameter names a project chooses - the secret-param class of the vocabulary decision
rules/native/rules-cross-layer/src/cross_layer/mod.rs:VERSION_SEGMENT_PATTERN convention -> vocabulary.apiVersionSegmentPattern # AMBIGUOUS - /v1/ is a widespread URL convention, not a standard; date-versioned APIs (/2024-01/) are invisible to it
rules/native/rules-cross-layer/src/cross_layer/prefix_drift.rs:MIN_PREFIX_DRIFT_GROUP cap
rules/native/rules-cross-layer/src/cross_layer/retrying_write_no_idempotency.rs:IDEMPOTENCY_GUARDED_ATTR internal # zzop producer/consumer attribute key
rules/native/rules-cross-layer/src/cross_layer/retrying_write_no_idempotency.rs:IDEMPOTENCY_ROLLOUT_LANDING internal # prescription-landing prose zzop emits, and the FIRST one outside rules-schema, where all ten of its siblings live: what "make the handler idempotent" costs when a reader does it correctly. The mechanism the message already names is a client-supplied Idempotency-Key, a keyless request leaves the handler nothing to dedupe on, so the edit that actually delivers this ends with the route rejecting keyless callers - and this rule can name exactly one of them, because its trigger (IoConsume::retry_configured) fires only on an axios-retry-wired file or a pRetry/backOff wrapper. Not `convention` - no project declares this, it is zzop's own sentence; a const rather than an inline string so the §27 position pin has one spelling to compare an index against. Deliberately NOT shared with REMOVAL_LANDING despite both being "the edit changes a contract other parties depend on": that one is a class-scoped DTO edit and names no header, and reusing the nearest sibling is the trap §37 records
rules/native/rules-cross-layer/src/cross_layer/retrying_write_no_idempotency.rs:RETRY_WITNESS_EXTENSIONS fact # file extensions fixed by the languages - the TS dispatch arm spelled in the rule crate for the sightline declaration, pinned both directions by zzop_engine::sightlines' ts_witness_extension_lists_match_the_dispatch_table
rules/native/rules-cross-layer/src/cross_layer/sdk_import_no_visible_consume.rs:MIN_OPAQUE_CLIENT_IMPORTING_FILES cap
rules/native/rules-cross-layer/src/cross_layer/sdk_import_no_visible_consume.rs:MIN_SDK_IMPORTING_FILES cap
rules/native/rules-cross-layer/src/cross_layer/sdk_import_no_visible_consume.rs:OPAQUE_HTTP_CLIENT_PATTERN fact # published npm package names
rules/native/rules-cross-layer/src/cross_layer/sdk_import_no_visible_consume.rs:SDK_SPECIFIER_PATTERN fact # published npm package names plus their segment shapes
rules/native/rules-cross-layer/src/cross_layer/sensitive_response_field.rs:RESPONSE_WITNESS_EXTENSIONS fact # file extensions fixed by the languages - the TS dispatch arm spelled in the rule crate for the sightline declaration, pinned both directions by zzop_engine::sightlines' ts_witness_extension_lists_match_the_dispatch_table
rules/native/rules-cross-layer/src/cross_layer/sensitive_response_field.rs:SENSITIVE_RESPONSE_FIELD_EXACT convention -> vocabulary.sensitiveResponseFieldExactNames # what a project names its sensitive response fields is its choice - same class as SECRET_PARAM_NAMES
rules/native/rules-cross-layer/src/cross_layer/sensitive_response_field.rs:SENSITIVE_RESPONSE_FIELD_SUBSTRINGS convention -> vocabulary.sensitiveResponseFieldSubstrings # same class/rationale as SENSITIVE_RESPONSE_FIELD_EXACT (one vocabulary, three match axes)
rules/native/rules-cross-layer/src/cross_layer/sensitive_response_field.rs:SENSITIVE_RESPONSE_FIELD_SUFFIXES convention -> vocabulary.sensitiveResponseFieldSuffixes # same class/rationale as SENSITIVE_RESPONSE_FIELD_EXACT (one vocabulary, three match axes)
rules/native/rules-cross-layer/src/cross_layer/sensitive_response_field/message.rs:REMOVAL_LANDING internal # prescription-landing prose zzop emits: what removing the field costs, as distinct from the disqualifier this rule already carried about whether the finding is true. Two facts, neither previously stated - the finding is ROUTE-scoped while both remedies (drop the property, or @Exclude() it) are CLASS-scoped, so they land on every other route declaring the same return type including one where handing the value out is the point; and nothing fails to build afterwards, because an HTTP consume site is not type-checked against the provider's DTO. Not `convention` - no project declares this, it is zzop's own sentence; a const rather than an inline string so the §27 position pin has one spelling to compare an index against, and in its own module because the parent file stood at 299 of the 300-line cap. States a MECHANICAL property of class-transformer and of deleting a class property, never a frequency: whether a shared DTO is common is unmeasured, and §30/§32 both record that teaching a reader to doubt true findings costs more than the one it saves
rules/native/rules-cross-layer/src/cross_layer/unconsumed_endpoint.rs:EXTERNALLY_FETCHED_PATHS convention -> vocabulary.externallyFetchedPaths # AMBIGUOUS - /robots.txt and /favicon.ico are standards, but /health, /healthz, /livez are ops conventions each project names itself
rules/native/rules-cross-layer/src/cross_layer/unconsumed_endpoint.rs:MAX_LISTED_PER_SOURCE cap
rules/native/rules-cross-layer/src/cross_layer/unconsumed_endpoint.rs:WELL_KNOWN_PREFIX fact # /.well-known/ is fixed by RFC 8615
rules/native/rules-cross-layer/src/cross_layer/unconsumed_mutation_endpoint.rs:RULE_ID internal # zzop rule id
rules/native/rules-cross-layer/src/cross_layer/write_methods.rs:CROSS_LAYER_WRITE_METHODS fact # HTTP method TOKENS fixed by RFC, not names a project picks. T2 - pinned equal to rules-http WRITE_HTTP_METHODS in engine policy_pins.rs (this crate depends on zzop-core alone)
rules/native/rules-cross-layer/src/lib.rs:NATIVE_ANALYSES internal # zzop's own native analysis IDS paired with the io channels each rule reads — one table, read by this crate's register_native_analyses AND its native_rule_channels, so an id cannot be registered without a channel statement. A row silently dropped unregisters the rule and un-declares it in one edit; rule_contracts::rule_channels binds each row's channels to the io its module actually compares
rules/native/rules-graph/src/cache_lane_file_read.rs:DEFAULT_FILE_READ_CALLEES convention -> vocabulary.fileReadCallees # stdlib read spellings, not names a project picks - declarable so a tree whose read_to_string means something else can narrow it
rules/native/rules-graph/src/cache_lane_file_read/tests.rs:ANCHOR test
rules/native/rules-graph/src/cache_lane_file_read/tests.rs:READS test
rules/native/rules-graph/src/dead_candidates.rs:DEAD_MAX_CHANGES cap
rules/native/rules-graph/src/lib.rs:NATIVE_ANALYSES internal # zzop's own native analysis IDS paired with the io channels each rule reads — one table, read by this crate's register_native_analyses AND its native_rule_channels, so an id cannot be registered without a channel statement. A row silently dropped unregisters the rule and un-declares it in one edit; rule_contracts::rule_channels binds each row's channels to the io its module actually compares
rules/native/rules-http/src/duplicate_route/boundary.rs:DEPLOYMENT_MANIFEST_NAMES fact # each ecosystem's BUILD TOOL fixed these filenames — Maven mandates `pom.xml`, Composer mandates `composer.json`, Cargo mandates `Cargo.toml`. `fact`, not `convention`: a project does not get to pick them, so there is nothing for a config key to declare (that is what `check-convention-vocab-declarable.sh` asks of a `convention` row, and it is the right question — it caught this row mis-axed on 2026-08-20). The list is still the §2 hazard in the other direction: an ecosystem with no row is outside it forever, which is exactly how Maven came to be missing while four Spring Boot apps on four ports were told to merge their handlers. The declaration states what it cannot see rather than leaving the gap silent
rules/native/rules-http/src/duplicate_route/boundary.rs:DEPLOYMENT_MANIFEST_SUFFIXES fact # MSBuild fixed `.csproj`/`.fsproj`/`.vbproj`. Split from the names list only because these match by extension rather than whole filename — same axis, same owner, same reason as DEPLOYMENT_MANIFEST_NAMES
rules/native/rules-http/src/http_scan.rs:NEAR_MISS_STEM_PATTERN internal # grammar of zzop's own marker
rules/native/rules-http/src/http_scan.rs:OK_MARKER_LOOKBACK_ABOVE cap
rules/native/rules-http/src/http_scan.rs:OK_MARKER_LOOKBACK_LINES cap
rules/native/rules-http/src/http_scan.rs:OK_MARKER_SPELLING internal # spelling of zzop's own suppression marker
rules/native/rules-http/src/http_scan.rs:SAFE_METHODS fact # HTTP method tokens
rules/native/rules-http/src/http_scan.rs:WRITE_HTTP_METHODS fact # read the decl - HTTP method TOKENS, not method NAMES a project picks
rules/native/rules-http/src/http_scan.rs:WRITE_SITE_COVERED_EXTENSIONS fact # file extensions fixed by the languages
rules/native/rules-http/src/http_scan/tests.rs:MARKER_WINDOW_PROSE_PAGES test
rules/native/rules-http/src/http_scan/tests.rs:SIGHTLINE_PROSE_PAGES test
rules/native/rules-http/src/lib.rs:NATIVE_ANALYSES internal # zzop's own native analysis IDS paired with the io channels each rule reads — one table, read by this crate's register_native_analyses AND its native_rule_channels, so an id cannot be registered without a channel statement. A row silently dropped unregisters the rule and un-declares it in one edit; rule_contracts::rule_channels binds each row's channels to the io its module actually compares
rules/native/rules-http/src/mutating_route_no_auth.rs:AUTH_GUARDED_ATTR internal # zzop producer/consumer attribute key
rules/native/rules-http/src/mutating_route_no_auth.rs:CALL_GRAPH_COVERED_EXTENSIONS fact # file extensions fixed by the languages
rules/native/rules-http/src/mutating_route_no_auth.rs:DEFAULT_AUTH_GUARD_PATTERN convention -> vocabulary.authGuardPattern # how a project spells its own guard functions - the headline case of the vocabulary decision; DEFAULT_ already concedes it
rules/native/rules-http/src/mutating_route_no_auth/qualifier.rs:QUALIFIER_GUARD_TOKENS convention -> vocabulary.authGuardQualifierTokens # camel tokens of project-authored guard CLASS names; its own doc admits a finance SecurityRepository false-clear
rules/native/rules-http/src/mutating_route_no_auth/vocab.rs:AUTH_ACQUISITION_CONDITIONAL_PATTERN convention -> vocabulary.authAcquisitionConditionalPattern # URL path segments a project names (/register, /otp, /refresh)
rules/native/rules-http/src/mutating_route_no_auth/vocab.rs:AUTH_ACQUISITION_STANDALONE_PATTERN convention -> vocabulary.authAcquisitionStandalonePattern # URL path segments a project names (/login, /signin)
rules/native/rules-http/src/mutating_route_no_auth/vocab.rs:AUTH_FAMILY_PATH_PATTERN convention -> vocabulary.authFamilyPathPattern # URL path segments a project names
rules/native/rules-http/src/route_shadowing.rs:FIRST_MATCH_ROUTER_EXTENSIONS fact # file extensions fixed by the languages
rules/native/rules-http/src/unprovided_consume/fold.rs:MIN_FOREIGN_UNPROVIDED_GROUP cap
rules/native/rules-http/src/unprovided_consume/vocab.rs:ALWAYS_VETO_EXTENSION_PATTERN fact # asset file extensions fixed by the formats
rules/native/rules-http/src/unprovided_consume/vocab.rs:API_SEGMENT_PATTERN convention -> vocabulary.apiSegmentPattern # /api, /rpc, /v1 are URL segment names each project chooses
rules/native/rules-http/src/unprovided_consume/vocab.rs:ASSET_DIR_GATED_EXTENSION_PATTERN fact # file extensions fixed by the formats
rules/native/rules-schema/src/lib.rs:NATIVE_ANALYSES internal # zzop's own native analysis IDS paired with the io channels each rule reads — one table, read by this crate's register_native_analyses AND its native_rule_channels, so an id cannot be registered without a channel statement. A row silently dropped unregisters the rule and un-declares it in one edit; rule_contracts::rule_channels binds each row's channels to the io its module actually compares
rules/native/rules-schema/src/lib.rs:SCHEMA_ISSUE_NAMESPACE internal # zzop rule-id namespace
rules/native/rules-schema/src/lib.rs:SCHEMA_STRUCTURAL_ISSUE_LABELS internal # zzop issue labels
rules/native/rules-schema/src/lib.rs:SCHEMA_USAGE_ISSUE_LABELS internal # zzop issue labels
rules/native/rules-schema/src/message/index_build.rs:CONCURRENT_INDEX_EXIT internal # the EXIT both index rules publish after INDEX_BUILD_LANDING, shared byte-identically because it is one procedure and neither rule's subject changes a word of it: on a large table, hand-edit the generated migration to CREATE INDEX CONCURRENTLY IF NOT EXISTS, which takes no write lock but cannot run inside a transaction block (so it must be alone in its migration file) and leaves an INVALID index behind if the build fails. Not `convention` — no project declares this, it is zzop's own sentence; a const rather than an inline string because the §27 position pins need one spelling to compare an index against, and because two copies would let the two rules drift into describing one CREATE INDEX differently
rules/native/rules-schema/src/message/index_build.rs:FK_INDEX_NO_READER_EXIT internal # the exit only fk-no-index publishes — that rule reads the field's NAME and no query at all, so an index it prescribes may be pure write cost on a column nothing filters by. Deliberately NOT shared with orderby-unindexed (rule-quality.md §30): that rule is anchored at a call site already sorting on the column, so its own finding has answered the question this sentence asks, and splicing it there would undo what the reader was just told — the same trap NOT_NULL_LANDING avoids by not reusing FK_CONSTRAINT_LANDING. Not `convention`; a const so the position pin, and the refusal pin that asserts orderby-unindexed does NOT carry it, both have one spelling
rules/native/rules-schema/src/message/index_build.rs:INDEX_BUILD_LANDING internal # prescription-landing prose zzop emits, and the FIRST on the AVAILABILITY axis rather than the failure axis the five in landing.rs beside it share: CREATE INDEX destroys no data, breaks no deploy and leaves the code correct — what it costs is a window in which writes to one table wait. Prisma's diff never generates the concurrent form, so `@@index([...])` becomes a plain CREATE INDEX built under a SHARE lock; count the rows to know whether that is milliseconds or a deploy window. Not `convention` — no project declares this, it is zzop's own sentence; one const shared by fk-no-index (both arms) and orderby-unindexed because the mechanism belongs to the DDL rather than to what either rule detects, and a new const rather than a reuse of COLUMN_TYPE_CHANGE_LANDING because that sibling names ACCESS EXCLUSIVE, a full table rewrite and a mid-deploy value failure, all three FALSE of CREATE INDEX
rules/native/rules-schema/src/message/landing.rs:COLUMN_TYPE_CHANGE_LANDING internal # prescription-landing prose zzop emits (same axis as MIGRATION_LANDING/FK_CONSTRAINT_LANDING/NOT_NULL_LANDING/DATA_LOSS_LANDING beside it): what happens when a reader acts on float-money or temporal-as-string and changes a column's declared TYPE — `prisma migrate` emits a bare ALTER COLUMN ... SET DATA TYPE with no USING and no backfill, and Postgres answers it by taking an ACCESS EXCLUSIVE lock and rewriting the whole table, failing mid-deploy on any stored value the cast cannot produce. Not `convention` — no project declares this, it is zzop's own sentence; one const shared by two rules because the mechanism belongs to the DDL rather than to what either rule detects and only the EXIT differs, and because the §27 position pins need a single spelling to compare an index against
rules/native/rules-schema/src/message/landing.rs:DATA_LOSS_LANDING internal # prescription-landing prose zzop emits, the third one (same axis as MIGRATION_LANDING/FK_CONSTRAINT_LANDING beside it): what the reader destroys by acting on god-model, unreferenced-field-name or unreferenced-model-name — `prisma migrate` diffs the schema and emits DDL only, so whatever leaves a model becomes DROP COLUMN and whatever leaves the schema becomes DROP TABLE, with no backfill and no way back. Not `convention` — no project declares this, it is zzop’s own sentence; one const shared by three rules because the mechanism is one mechanism and only the EXIT differs, and because the §27 position pins need a single spelling to compare an index against
rules/native/rules-schema/src/message/landing.rs:DATETIME_MIGRATION_EXIT internal # the EXIT temporal-as-string publishes after COLUMN_TYPE_CHANGE_LANDING — the sibling of DECIMAL_MIGRATION_EXIT and separate from it because the two differ in the fact that decides the whole edit: Postgres has no implicit text-to-timestamp cast, so the generated statement is rejected while still being planned and needs a hand-written USING, which then aborts on the first unparseable value. Not `convention`; a const rather than an inline string because message.rs sits at the 300-line cap and because the position pin needs one spelling
rules/native/rules-schema/src/message/landing.rs:DECIMAL_MIGRATION_EXIT internal # the EXIT float-money publishes after COLUMN_TYPE_CHANGE_LANDING — ship the generated migration on a small table (float8 to numeric is an ASSIGNMENT cast, so it needs no USING — but a narrowed @db.Decimal(p, s) still aborts the ALTER with `numeric field overflow` on a stored amount wider than p - s digits, which is why the exit prescribes the bare Decimal), or add a second Decimal field and backfill in batches; plus the fact no reader can get from the schema, that the amounts already stored were rounded when they were WRITTEN and the cast cannot make them exact. Not `convention`; a const rather than an inline string because message.rs sits at the 300-line cap and because the position pin needs one spelling
rules/native/rules-schema/src/message/landing.rs:FIELD_RETIREMENT_EXIT internal # the EXIT unreferenced-field-name publishes after DATA_LOSS_LANDING — dump the column, retire it over two releases, or leave it declared. Not `convention`; a const rather than an inline string because message.rs sits at the 300-line cap and because the position pin needs one spelling
rules/native/rules-schema/src/message/landing.rs:FK_CONSTRAINT_LANDING internal # prescription-landing prose zzop emits, the second one (same axis as MIGRATION_LANDING beside it): what happens when a reader models the implicit relation this rule reports — Postgres validates the generated FOREIGN KEY against the rows already in the table, so `prisma migrate deploy` stops mid-deploy on the first value with no parent. Not `convention` — no project declares this, it is zzop's own sentence; a const rather than an inline string so the §27 position pin has one spelling to compare an index against
rules/native/rules-schema/src/message/landing.rs:MIGRATION_LANDING internal # prescription-landing prose zzop emits (same axis as framework_silence's FUNNEL_TAIL): how a missing-timestamps edit lands in a migration, spliced ahead of the imperative in every arm of that rule. Not `convention` — no project declares this, it is zzop's own sentence; a const rather than one copy per arm because the arm that shipped no prose at all is exactly the defect it closes
rules/native/rules-schema/src/message/landing.rs:MODEL_RETIREMENT_EXIT internal # the EXIT unreferenced-model-name publishes after DATA_LOSS_LANDING — FIELD_RETIREMENT_EXIT at table granularity (DROP TABLE, rows). Two consts rather than one parameterised sentence: the halves differ in more than a noun, and a template read twice is how the wrong half reaches a reader
rules/native/rules-schema/src/message/landing.rs:NOT_NULL_LANDING internal # prescription-landing prose zzop emits, the fourth one (same axis as MIGRATION_LANDING/FK_CONSTRAINT_LANDING/DATA_LOSS_LANDING beside it): what happens when a reader answers nullable-fk with "optional was not intentional" and drops the `?` — Prisma emits ALTER COLUMN ... SET NOT NULL, which Postgres validates against every row already stored, so `prisma migrate deploy` stops mid-deploy on the first NULL. Not `convention` — no project declares this, it is zzop's own sentence; a fourth const rather than a reuse of FK_CONSTRAINT_LANDING because that sibling's own exit ("backfill the parentless values to NULL and make the column optional") is the state THIS reader is leaving, and because the §27 position pin needs one spelling to compare an index against
rules/native/rules-schema/src/message/landing.rs:NULLABLE_FK_EXIT internal # the EXIT nullable-fk publishes after NOT_NULL_LANDING — both ways out named, because for this rule leaving the column optional is frequently the right answer (two mutually exclusive parents on one model hold NULL in one of them on every row by design). Not `convention`; a const rather than an inline string because message.rs sits at the 300-line cap and because the position pin needs one spelling. Carries the RUNTIME half of the promotion too: Prisma's implicit onDelete is SetNull for an optional relation and Restrict for a required one
rules/native/rules-schema/src/message/sightline.rs:QUERY_CALL_SITE_EXTENSIONS fact # file extensions fixed by the languages - the TS dispatch arm spelled in the rule crate for the sightline declaration, pinned both directions by zzop_engine::sightlines' ts_witness_extension_lists_match_the_dispatch_table
rules/native/rules-schema/src/message/tests.rs:SIGHTLINE_PROSE_PAGES test
rules/native/rules-schema/src/structural.rs:GOD_MODEL_MEASUREMENT internal # the one owner of the MEASUREMENT behind GOD_THRESHOLD, in the sentence the finding's message splices and both published rule pages are pinned against (`crate::message::tests::the_god_model_threshold_is_identical_in_the_constant_and_the_published_docs`). Warning/evidence prose zzop emits -- same axis as `framework_silence/builtin_fetch.rs:FUNNEL_TAIL` and `message/landing.rs:MIGRATION_LANDING`, and no project spells any of it. The NUMBERS in it are deliberately not restated on this line: it exists because they were hand-copied into three prose surfaces at once, and a census tail is the one place no guard could ever compare them
rules/native/rules-schema/src/structural.rs:GOD_THRESHOLD cap # field count at or above which schema/god-model reports. A CONVENTION, not a measurement, and the declaration says so at length — the numbers are deliberately NOT restated here, because this guard compares key and axis only and can never read a tail (`crates/engine/.../uncovered_extension.rs:THIN_RULE_REACH_PCT` carried a stale copy in exactly this slot until 2026-08-31). The measurement, the reason it was not raised to the distribution's own gap, and the recount all live beside the constant, and `GOD_MODEL_MEASUREMENT` there is the single owner the finding's message and both published pages are pinned against
rules/native/rules-schema/src/structural.rs:MONEY_TOKENS convention -> vocabulary.moneyTokens # money field names a project chooses - the amount-field class of the vocabulary decision
rules/native/rules-schema/src/structural.rs:STRUCTURAL_RULES_VERSION internal
rules/native/rules-schema/src/usage.rs:BOUND_MODEL_ATTR internal # zzop producer/consumer attribute key
rules/native/rules-schema/src/usage.rs:CHURN_CRITICAL_THRESHOLD cap # critical escalation of the model-churn ladder. A round number that CANNOT be measured from here: the count is injected (MODEL_CHURN_ATTR) and no native analysis produces one, so the rule reports 0 across all nine corpus trees and moving either rung moves nothing. Paired with CHURN_WARNING_THRESHOLD; the rule's own message now tells whoever HAS a churn distribution to set both from it
rules/native/rules-schema/src/usage.rs:CHURN_WARNING_THRESHOLD cap # reporting rung of the model-churn ladder — see CHURN_CRITICAL_THRESHOLD, neither is meaningful alone and neither has a population to calibrate against
rules/native/rules-schema/src/usage.rs:MODEL_CHURN_ATTR internal # zzop producer/consumer attribute key
rules/native/rules-schema/src/usage.rs:SKIP_FIELD_NAMES convention -> vocabulary.schemaUsageSkipFields # id/createdAt/updatedAt are schema field names a project chooses (created_at, inserted_at)
rules/native/rules-schema/src/usage/tokens.rs:FIELD_USAGE_SCAN_EXTENSIONS fact # file extensions fixed by the languages
